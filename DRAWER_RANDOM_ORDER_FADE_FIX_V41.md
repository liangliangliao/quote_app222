# 首页左侧菜单栏随机顺序与渐显修复 V41

## 修改文件
- `lib/main.dart`

## 变更点
1. 每次打开首页左侧菜单栏时，所有模块会基于 `_drawerStyleSeed` 执行 Fisher-Yates 洗牌，模块显示顺序随机变化。
2. 抽屉关闭时提前生成下一次打开使用的随机种子，避免抽屉已展开后才重新洗牌造成闪跳；下一次打开第一帧就是新的随机顺序。
3. 新增 `_DrawerStaggeredReveal` 外层渐显包装器：
   - 按随机后的列表顺序依次渐显；
   - 单项动画 360ms；
   - 单项延迟约 `index * 32ms + 少量随机扰动`；
   - 最大延迟封顶 420ms，保证最后一个模块也在约 0.8 秒内完整显示。
4. 为避免旧版“打开后只显示少数模块/全空/堆叠”的问题：
   - 所有模块仍然一次性构建在 `SingleChildScrollView + Column` 中；
   - 每个模块保留固定 `SizedBox` 高度，布局空间始终存在；
   - 初始透明度保留 18%，不会出现完全空白等待；
   - 内部复杂卡片动效保持终态，只保留呼吸微浮动，外层统一处理渐显。
5. 随机种子使用 `microsecondsSinceEpoch`，降低快速反复打开时随机结果重复的概率。
