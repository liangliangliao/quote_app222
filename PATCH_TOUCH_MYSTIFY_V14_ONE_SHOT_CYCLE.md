# V14：单周期一次释放/余韵修复

本次只修复用户反馈的核心问题：短时间内多次出现释放和余韵，导致完整性交过程反复循环。

## 问题原因

之前加速演示逻辑使用：

```java
sec = elapsed % durationSec
```

这会让完整周期在几分钟内不断取模重启，所以释放、余韵会反复出现。

## 修复方式

### 1. 动态壁纸加速演示改为单次演示

现在加速演示使用：

```java
sec = min(elapsed, durationSec - 0.001)
```

含义：即使开启加速演示，也只是把一个完整周期压缩成几分钟，周期内只出现一次临界、一次释放、一次余韵；到达周期尾部后停留在分离阶段，不再循环。

### 2. 真实壁纸仍按天级周期运行

真实模式不使用取模。周期结束后才生成下一轮新周期。默认仍然是 1～365 天随机。

### 3. 设置页预览也改成单次周期

设置页预览不再 `seconds % 36` 循环，而是 360 秒单次演示，避免用户看到几分钟内反复释放/余韵。

## 当前周期逻辑

每一轮周期：

1. 随机生成周期天数，默认 1～365 天。
2. 随机生成本轮释放时刻。
3. 临界紧贴释放之前。
4. 释放只出现一次。
5. 余韵只紧跟释放出现一次。
6. 余韵结束后回到分离，直到下一轮周期。

