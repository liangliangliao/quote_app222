# V28 桌面小组件直达表单修复

## 修复目标

针对桌面小组件体验问题做调整：

1. 长按桌面/应用图标时，不同手机桌面启动器的小组件入口不一致，不能依赖“长按应用图标一定出现小组件入口”。
2. 桌面小组件中的七层按钮点击后不应再弹出一条通知，而应直接进入对应层面的轻量表单。

## 本次改动

### 1. 小组件按钮直达轻量表单

修改 `BehaviorObservationWidgetProvider.kt`：

- 原逻辑：点击小组件层面按钮 → 发送可输入通知 → 用户再点通知输入。
- 新逻辑：点击小组件层面按钮 → 直接启动 `MainActivity` → 通过行为观察 payload 打开 `BehaviorObservationNotificationFormPage`。

传入 payload：

- `templateKey = notification_layer_select`
- `entryMode = desktop_widget_direct_form`
- `primaryLayer = 用户点击的层面`
- `source = desktop_widget`

因此点击：

- 行为 → 打开行为层面表单
- 时间 → 打开时间层面表单
- 情绪 → 打开情绪层面表单
- 认知 → 打开认知层面表单
- 结果 → 打开结果层面表单
- 环境 → 打开环境层面表单
- 身体 → 打开身体状态表单

### 2. 冷启动兜底

修改 `native_guard.dart`：

- App 冷启动时，原生可能先收到小组件点击，Flutter MethodChannel 尚未准备好。
- 现在 Flutter 通知处理器安装后，会主动消费一次原生暂存的行为观察 payload。
- 避免小组件点击后只进入首页或丢失跳转。

### 3. 小组件说明文案更新

- 小组件内部提示从“点选层面 → 通知输入 → 保存”改为“点选层面 → 打开对应表单”。
- 数据源页说明改为：优先使用 App 内“添加桌面小组件”按钮；如果桌面启动器不支持，请从桌面空白处的小组件入口添加。

### 4. 小组件元信息增强

更新 `behavior_observation_widget_info.xml`：

- 增加 previewImage。
- 增加 description。

这样在部分桌面启动器的小组件列表中更容易识别。

## 关于“长按桌面没显示添加小组件入口”

Android 不同桌面启动器差异很大：

- 有些支持长按应用图标显示“小组件”；
- 有些只显示“应用信息/卸载”；
- 有些必须长按桌面空白处，再进入“小组件/添加工具”；
- 有些国产 ROM 会把入口放在“添加工具/原子组件/桌面挂件”等名称下。

因此本版本同时保留 App 内：

行为观察 → 数据源 → 桌面小组件快速记录 → 添加桌面小组件

作为更稳定的入口。

## 影响范围

- 不改变行为观察数据结构。
- 不删除通知快速记录能力，只是桌面小组件默认不再走通知输入。
- 已有小组件更新后需要等待桌面刷新，必要时删除旧小组件后重新添加。
