# PATCH_TOUCH_MYSTIFY_V157_CONTINUOUS_MORPHING_TRACE

本次继续基于用户提供的标准参考视频，主动检查 V156 源码与参考效果之间的重大差距，并进行改造。

## 一、V156 仍存在的主要差距

### 1. 每段短句仍然固定 motif
V156 每一轮通过 `v156MotifIndex(...)` 选择一个固定 motif，例如长丝带、扇形、花瓣、薄翼、环形网纱或极简光弧。

这样虽然避免了乱线团，但实际效果仍容易变成：

- 这一段一直是丝带；
- 下一段一直是扇形；
- 再下一段一直是网纱。

这不符合参考视频中“同一段光迹内部不断变化”的感觉。

### 2. 变化仍偏短句之间切换，而不是主体内部演化
参考视频里，形态更像是在同一段正在创作的光痕中继续长出来：

- 先是细线；
- 头部继续展开；
- 局部生成扇面或薄翼；
- 旧部分逐渐淡去；
- 新头部继续产生新的形态。

V156 的变化更像一段一段地选择不同主题，而不是同一主体内部的连续演化。

### 3. 先画先消失仍不够明确
虽然 V156 有 `head/tail`，但视觉上旧部分的退隐仍不够像“时间绘画”。

---

## 二、V157 改造方向

V157 不再继续增加更多模板，而是重写当前主路径为：

```text
同一主体 + 按出生时间分配笔法 + 头部持续生成 + 尾部持续退隐
```

也就是说，同一段主体内部会按 `u` 位置和局部出生时间连续演化，而不是整段固定一个 motif。

---

## 三、主要新增方法

新增主入口：

- `drawReferenceVideoTimePainterV157(...)`

新增生命周期控制：

- `v157PhraseLife(...)`
- `v157Head(...)`
- `v157Tail(...)`
- `v157PhraseCenter(...)`

新增连续变形主体：

- `drawV157EvolvingSubject(...)`
- `buildV157Lane(...)`
- `v157StyleValue(...)`
- `v157Point(...)`
- `v157PointForStyle(...)`
- `drawV157SubtleFieldLines(...)`
- `drawV157BrushTip(...)`

---

## 四、V157 的核心机制

### 1. 取消“一段固定 motif”
V156 是一段选择一个固定 motif。

V157 改为：同一段内不同位置按 `u` 逐渐获得不同形态。

### 2. 形态由出生时间生成
`v157StyleValue(...)` 以 `u` 为主，`local` 为辅：

- 旧部分保留较早的笔法；
- 新头部继续生成新的笔法；
- 整体形成“线条 → 扇面 → 场线 → 丝带 → 花瓣 → 环形光弧”的连续过渡。

### 3. 形态之间使用连续插值
`v157Point(...)` 不直接切换完整模板，而是在相邻形态之间做 `smoother(fract(styleValue))` 插值。

这样避免：

```text
线条模板突然替换成扇形模板
```

而改为：

```text
线条局部逐渐打开为扇面，再继续变成场线/丝带/花瓣
```

### 4. 减少回声，避免复制主体
V157 只保留一层很淡的同源时间回声，用于表达旧痕迹退隐，不再重画多个完整主体。

---

## 五、参考技术原则

结合 XScreenSaver / rss-glx 等开源屏保的生成式思路，V157 更强调：

- 每帧持续生成，而不是播放预制形状；
- 图形由状态和时间演化产生；
- 当前痕迹和旧痕迹叠加形成时间感；
- 控制复杂度，避免乱线团。

---

## 六、当前主路径

当前主路径已从：

```java
 drawReferenceVideoTimePainterV156(...)
```

切换为：

```java
 drawReferenceVideoTimePainterV157(...)
```

V156 旧方法暂时保留，方便回退对比，但当前不再作为主渲染入口。
