# 健康饮食语音输入：聊天式分段听写修复

## 问题
系统语音识别在部分 Android 设备上不适合长时间连续听写，用户说完一句以后继续说，后续内容可能无法继续识别，或者只保留第一句/最后一句。

## 处理
将每日饮食语音输入改为类似聊天软件的“分段语音消息”设计：

1. 一句结束后立即保存为一个语音片段。
2. 自动开启下一段识别，避免只保留上一轮结果。
3. UI 显示“已保存语音片段”，可删除误识别片段。
4. 降低 pauseFor 到 2 秒，让系统更快结束当前句并续听。
5. 每轮 listenFor 限制为 28 秒，避免长时间卡在同一轮。
6. 移除每轮开始前强制 cancel，降低国产 ROM 上 error_busy 概率。
7. 增加 force commit：如果系统长时间不返回 finalResult，7 秒后先保存当前稳定内容。
8. 保留手动编辑入口，最终仍以用户确认文字为准。

## 修改文件
- lib/health_diet/daily_share/diet_voice_input_page.dart
