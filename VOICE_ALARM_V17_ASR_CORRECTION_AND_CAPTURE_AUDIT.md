# VOICE_ALARM_V17_ASR_CORRECTION_AND_CAPTURE_AUDIT

## 本轮重点

用户反馈：
1. 语音修正仍然感觉不起作用；
2. 语音识别可能漏掉用户说的某些话。

本轮在 v16 基础上继续修复两个方向：

- 讯飞动态修正协议处理更保守、更接近真正的 `pgs=rpl` 替换语义；
- 云识别收音不再过度依赖本地硬阈值 VAD，降低漏掉弱音/开头词/短句的概率。

## 修复 1：讯飞 rpl 替换范围防旧包覆盖

v16 已经支持 `pgs=apd/rpl` 与 `rg=[start,end]`，但仍有一个遗漏：

当 `rpl` 修正包到达后，如果旧的 `apd` 包因为网络/线程调度晚到，旧包可能再次覆盖已经修正的片段，导致用户看起来“修正没生效”。

v17 增加：

- `iflytekProtectedReplaceRanges`；
- 当 `pgs=rpl` 到来时，删除 `rg` 范围内旧片段；
- 将替换结果放在 `rg.start` 位置；
- 对该 `rg` 范围加保护；
- 后续迟到的普通 `apd` 如果落在已替换范围内，不再覆盖修正文本。

## 修复 2：兼容不同 rg 形态

部分实现或中间层可能把 `rg` 作为 JSON 数组返回，也可能作为字符串形式返回。

v17 增加：

- `parseIflytekRange(result)`；
- 支持 `JSONArray`；
- 支持字符串中提取数字范围；
- 自动做 `min/max`，避免范围反向导致替换失败。

## 修复 3：讯飞普通场景改为打开 WebSocket 后持续送音频

v16 仍然是：本地 VAD 检测到人声后才开始上传音频。问题是：

- 用户声音较轻；
- 手机麦克风增益较低；
- 开头几个字音量低；
- 用户说话很快；
- 本地峰值阈值没有触发；

这些情况下，本地还没确认 speechStarted，用户的话已经开始了，可能造成开头词丢失，甚至整句被丢。

v17 改为：

- 非播报场景下，讯飞 WebSocket 打开后就持续上传音频帧；
- 让讯飞服务端自己的 VAD/语言模型看到完整声学上下文；
- 本地 VAD 仍用于判断起止、超时、避免空转；
- AI/TTS/闹钟播报期间仍保持更严格门控，降低外放回声。

## 修复 4：降低本地 VAD 漏检概率

原先阈值偏保守：

- 普通场景 peak >= 520；
- 播报场景 peak >= 2200；

部分设备或轻声说话容易漏检。

v17 调整为：

- 普通场景 peak >= 260；
- 播报场景 peak >= 1500；
- 播报结束交接窗口 peak >= 180；
- 仍保留连续命中机制，避免单帧噪声过度触发。

## 修复 5：pre-roll 从 420ms 增强到 900ms/1200ms

为了减少“刚开口第一个词丢失”，v17 扩大预录缓存：

- 普通场景：约 900ms；
- 播报/交接场景：约 1200ms；

一旦确认是真人语音，将开口前缓存音频一起送入识别。

## 修复 6：短语音不再被最小音频长度过度过滤

Microsoft REST 录音块原来至少约 400ms 才保留。短句、短确认、短命令可能被过滤。

v17 改为约 160ms 起保留，降低“好的、可以、确定、不要”等短句被直接丢弃的概率。

## 修复 7：Microsoft 队列容量提升

Microsoft 当前仍是 REST 识别，不是真正实时动态修正。为了减少网络慢导致队列溢出，队列容量从 16 提升到 32。

注意：Microsoft 当前能同步“自动提交/文本稳定/收音队列”，但无法做到讯飞 `dwa=wpgs` 那种实时 rpl 动态修正。要达到同等效果，后续必须接入 Azure Speech SDK / WebSocket continuous recognition，而不是 REST 单段识别。

## 修改文件

- android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt

## 静态检查

- Kotlin 括号数量检查通过；
- Dart 括号数量检查通过；
- AndroidManifest.xml 解析通过；
- zip 根目录无多级嵌套。

## 仍需真机验证的点

1. 讯飞返回 `pgs=rpl` 时，UI 是否出现“讯飞已动态修正”；
2. 轻声说话、短句、快速接话是否仍丢开头字；
3. AI/TTS 播报期间是否产生过多外放回声误识别；
4. Microsoft REST 模式是否仍存在网络慢导致的转写延迟。
