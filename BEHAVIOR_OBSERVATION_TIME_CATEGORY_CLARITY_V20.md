# V20 行为观察：时间去向与待确认语义修复

## 这次修复的核心问题

复盘页不应让用户看到一个含糊的「待确认」大类并占用大量时间，因为用户真正关心的是：这些时间到底来自哪里、是什么行为或数据源产生的。

## 已完成改造

1. **屏幕使用时间统一归类**
   - 所有来自 UsageStatsManager / screen_usage / 屏幕使用 / 今日使用 / 使用应用 的记录，在复盘页统一显示为「屏幕使用时间」。
   - 不再把手机 App 使用时间拆到娱乐、工作/学习、人际、其他或待确认中。

2. **待确认不再被误当作行为类别**
   - 「待确认」被解释为：自动导入数据尚未确认，不是一个行为类别。
   - 复盘页待确认提示文案已改为：点击进入数据源页确认明细，可确认写入或忽略。
   - 自动导入待确认项不会作为含糊类别误导用户。

3. **时间去向地图同步更新**
   - 主页面只显示类别汇总：屏幕使用时间、工作/学习、生活事务等。
   - 点击类别后进入明细页查看全部具体行为。

4. **具体行为排行保留具体 App / 行为**
   - 屏幕使用时间作为一个类别；
   - 但具体行为排行中仍显示具体 App 或具体行为，帮助用户知道手机时间具体被谁占用。

5. **重复导入去重**
   - 对屏幕使用和日历导入做统计层去重，避免同一 App 同一天重复导入导致时间膨胀。
   - 队列保存也避免同一个 source + externalId 反复生成待确认项。

6. **类别列表新增「屏幕使用时间」**
   - 作为内置类别出现。
   - 历史遗留的「待确认」等导入状态类不再进入自定义类别筛选列表。

## 仍需真机验证

当前环境没有 Flutter / Android 编译环境，尚未执行真实 APK 编译和真机数据验证。建议在 GitHub Actions 或 Android Studio 中重新构建，并重点测试：

- 屏幕使用时间导入后是否统一显示为「屏幕使用时间」；
- 待确认队列点击是否进入数据源页；
- 同一天重复导入是否不再让复盘时间翻倍；
- 具体行为排行是否仍能显示具体 App。
