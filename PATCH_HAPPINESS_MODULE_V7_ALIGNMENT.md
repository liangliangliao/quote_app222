# 幸福模块 V7 对齐补丁

本次修复与补强针对以下问题：

1. 概念深度分析使用了重复、泛化、敷衍的经验直观/常见表现/练习描述。
2. 行为链没有按单元原课程内容对齐。
3. AI 生成行动虽然带了摘要，但没有明确要求与本单元原内容、概念、案例、内涵外延保持一致。
4. AI 只能生成 1 条行动，无法配置生成数量。
5. 需要默认优先调用 DeepSeek 的深度思考模型。

## 本次补丁内容

- 新增 `lib/happiness_module/happiness_training_specs.dart`
  - 提供按“单元 + 核心概念”生成的概念镜头说明
  - 提供按单元生成的行为链规格
- 单元详情页的“概念深度分析”改为读取单元级概念镜头，而非复用泛化文案
- 单元详情页的“行为链”改为读取单元级行为链规格，而非只按 tag 粗略生成
- AI 行动生成：
  - 明确喂给单元完整原内容、是什么、为什么、怎样做、课程案例、现实案例、默认行动
  - 支持一次生成 1~5 条行动
  - Prompt Studio 支持设置默认生成数量
  - DeepSeek key 存在时，模块默认优先使用 `deepseek-reasoner`
- AI 行动页改为显示多条可采纳方案

## 仍可继续深化的方向

- 39 单元全部补成更细的“概念内涵 / 外延 / 经验直观 / 现实表现 / 练习”结构
- 知行城的场景任务链继续深化成更长剧情与更强现实迁移
- 行动前 / 行动中 / 行动后闭环继续增强
