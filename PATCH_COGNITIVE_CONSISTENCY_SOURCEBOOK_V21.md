# PATCH_COGNITIVE_CONSISTENCY_SOURCEBOOK_V21

本补丁在 v20 源书认知一致性结构化落地基础上继续补齐“读表使用、交互修正、行动执行、复盘画像、Todo 自动提示”的真实实践闭环。

## P0 已落地

1. 清空数据补齐
- `clearAllCognitiveConsistencyData()` 已加入全部源书新表，避免用户清空模块后仍残留源书案例、节点、关系边、价值分层、选择记录、行动契约、反态度实验、人际平衡记录。

2. 源书表读取 API
- 新增 `sourcebookCaseDetail(sessionId)`。
- 新增 `sourcebookCaseDetailsForSessionIds(sessionIds)`。
- 源书页面加载 session 时同步读取源书结构化明细。

3. 源书结果优先使用结构化数据库数据
- `_sourcebookMap()` 不再只依赖 AI rawResponse。
- 如果数据库中存在节点、边、价值分层、选择项、人际平衡、反态度实验、行动契约、确认记录、证据、验证任务，则优先合并到页面结果卡片。

4. fallback 结构化补齐
- 本地兜底 AI 不再输出空 rawResponse。
- 新增 `_fallbackSourcebookRaw()`，即使 AI 请求失败，也能生成最小 `sourcebookAnalysis`，保证源书卡片、DAO 结构化保存与后续闭环可用。

5. 节点/关系可确认修正
- 源书认知节点支持：准确 / 需修正 / 不成立。
- 源书冲突/一致关系边支持：准确 / 需修正 / 不成立。
- DAO 新增 `updateSourcebookNodeUserStatus()` 与 `updateSourcebookEdgeUserStatus()`。

## P1 已落地

1. 源书案例详情
- 最近源书案例支持打开“完整案例档案”。
- 案例详情聚合展示：当前阶段、用户确认、认知节点、关系边、价值分层、用户选择、行动契约、行动证据、现实验证任务、整合性表达。

2. 用户自主选择区可交互
- `userChoiceOptions` 不再只是文本展示。
- 用户可点击“选择这个方向”，系统写入 selected 状态并推进案例到行动契约阶段。
- 可将选择方向写入 Todo。

3. 一致性行动契约可执行
- 行动契约卡片支持：设为当前行动、写入 Todo、完成并写入证据。
- 完成后自动写入 `cc_evidence_records`，并将契约状态更新为 completed，案例阶段推进到 `review_integration`。

4. 反态度实验复盘
- 反态度实验卡片支持“实验复盘”。
- 复盘可记录实验结果观察、新的自我信念、旧信念松动程度 0-10。

5. 人际平衡行动追踪
- 人际平衡卡片支持写入沟通/边界行动。
- 支持记录沟通结果、对方反应、边界变化，并写回结构化记录。

6. Todo 自动提示源书分析
- Todo 今日行动卡片会在失败、长时间无努力记录或高摩擦状态下自动提示：这可能不是单纯时间管理问题。
- 问题树节点会在失败、长期未进入行动闭环或缺少具体行动契约时自动提示进入源书工作台。

## P2 已落地

1. 长期画像统计补齐
- `modernPatternProfile()` 新增源书画像统计：
  - 源书案例数量
  - 高频核心价值
  - 高频身份信念
  - 高频外围解释
  - 用户选择方向分布
  - 已选择方向分布
  - 行动契约状态分布
  - 高频反态度实验旧信念
  - 高频人际失衡点

2. 报告页展示源书画像
- 现代模式画像卡片显示源书案例数量和上述源书统计维度。

3. 认知地图升级为“节点—边可修正地图”
- `cc_conflict_edges` 增加 `from_node_id`、`to_node_id`。
- 保存关系边时会根据 label 尝试回连节点 id，为后续可视化图谱打基础。

## 说明

当前环境缺少 dart/flutter 命令，无法执行真实 `flutter analyze` 或编译。已做静态结构检查：本次修改的主要 Dart 文件大括号/小括号/中括号结构均平衡；保留原模块入口、session、证据账本、Todo 回写、专项场景、报告系统，不做破坏性重构。
