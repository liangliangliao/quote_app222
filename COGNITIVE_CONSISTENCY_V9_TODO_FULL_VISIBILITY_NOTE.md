# 足下一致行动 V9：Todo/问题树显性化与系统级融合增强

本版本针对“源码里有方法但实机用户看不到、不好点、不能闭环”的问题继续升级。

## 关键增强

1. 修复潜在编译隐患
   - 检查并修正足下一致行动核心页面与 DAO 的新增代码。
   - 保留 V8 的预设价值可多选、全选预设、多价值组合面板。

2. Todo 全域入口增强
   - 今日行动卡片继续保留“一致行动”。
   - 普通行动步骤列表 `_ActionStepListTile` 新增“一致行动”。
   - Todo 行动树节点也通过 `_ActionStepListTile` 获得“一致行动 / 奖励降噪 / 诚实校验”入口。

3. Todo 卡片显性显示一致行动证据状态
   - 每条 Todo 行动卡 / 行动树步骤下显示：
     - 一致行动证据数量；
     - 最近一次一致行动证据；
     - 关联价值。
   - 不再只把证据藏在足下一致行动模块内部。

4. 问题树节点显性接入
   - 所有问题树节点显示一致行动证据状态。
   - 可执行节点显示“一致行动”。
   - 非可执行节点显示“失调分析”，也能进入认知失调雷达。
   - 问题树节点新增“奖励降噪 / 诚实校验”入口。

5. 来源—价值强关联
   - 新增 `cc_source_value_links`。
   - 保存证据后，除了 `evidence ↔ value`，还会建立：
     - Todo step ↔ value；
     - Problem node ↔ value；
     - 其他来源 ↔ value。
   - Todo / 问题树状态卡可直接展示关联价值。

6. 报告生成行动保留来源链路
   - 如果用户是从 Todo / 问题树进入足下一致行动，再由报告生成下一步行动，保存证据时仍会保留原来源并允许回写。

7. Todo 场景嵌入奖励降噪与诚实校验
   - Todo 今日行动卡、普通行动步骤、问题树节点均提供显性入口。
   - 进入对应 Tab 时会自动带入当前 Todo / 问题节点上下文。

## 实机验证点

- 价值罗盘页应看到“预设价值（可多选）”和“全选预设”。
- Todo 今日行动卡片下方应出现“一致行动证据”状态卡。
- 普通 Todo 行动步骤列表也应出现“一致行动 / 奖励降噪 / 诚实校验”。
- 问题树节点不管是否可执行，都应能看到一致行动证据状态；非可执行节点按钮文案为“失调分析”。
- 从 Todo / 问题树进入并保存证据后，返回对应卡片应能看到证据数量增加。

## 说明

当前运行环境没有 Flutter / Dart 命令，无法在容器中真实执行 `flutter analyze` 或 `flutter build apk`。本包已完成源码级改造、关键方法与括号平衡检查，仍建议本地或 CI 跑一次完整编译。
