# Semantic Embedding v14 修复说明

本次修复目标：修正 Embedding-only 模式把 `鸟 / 鱼` 这类仅同属大类的词误标为“高语义相关”的问题。

## 关键改动

1. Embedding-only 模式只使用真实 Embedding API 返回的原生向量 cosine：
   - 不调用 LLM
   - 不使用本地词典/上下位规则
   - 不使用关键词或词面相似度参与最终分数
   - 不使用 fallback

2. 删除快速模式中的“归一化语义分数”作为最终判定依据。
   - 页面现在显示 `原生Embedding余弦`。
   - 不再把 0.50 左右的原生 cosine 通过归一化拉高成 1.0。

3. 标签改为“向量相关”，避免误导为严格语义等价。
   - 高向量相关
   - 中等向量相关
   - 低向量相关

4. 阈值修正：
   - 默认中等向量相关阈值：0.45
   - 默认高向量相关阈值：0.65
   - 若旧配置中保存过过低的高阈值，例如 0.40，会在快速模式中自动提升为 0.65。

5. 页面新增/保留诊断字段：
   - 原生Embedding余弦
   - A/B 向量维度
   - LLM=false
   - 本地规则=false
   - Fallback=false
   - 词面相似仅诊断

## 重要边界

Embedding cosine 只表示“向量空间相关度”，不代表：

- 同义
- 等价
- 上下位关系
- 互斥关系
- 事实一致性

例如：`鸟 / 鱼` 可能有中等向量相关，因为它们同属动物/生物大类，但不应该显示为高语义等价或高度相似。

严格判断请使用“标准精判”或“严格事实一致”模式。
