# PATCH_TOUCH_MYSTIFY_V121_SCREENSAVER_ART_BRUSH

本次继续优化“发现之旅 / 生理赋能 / 触摸”动画主体。

## 一、为什么要继续重构

用户指出：当前效果仍然像“一条贪吃蛇在屏幕上表演”，而不是艺术家在屏幕上作画。

问题根源在于：

- 当前画笔仍然容易呈现为单条蛇形主线；
- 形状和线条之间仍然存在视觉分离；
- 运笔虽然有节奏参数，但底层结构还不够接近经典屏保那种“运动控制点 + 曲线串 + 历史残影”的艺术机制。

因此，V121 重点参考 Windows Mystify / Beziers / Ribbons 类屏保思路，对画笔底层机制做进一步重构。

---

## 二、技术方案转向

文件：
- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

### 1. 参考屏保式“运动控制点”

V121 不再简单地沿一条轨迹生成一个附着形状。

新思路：

- 使用一组连续运动的控制点；
- 每个控制点有自己的轻微相位、方向、速度与偏移；
- 多个控制点共同形成一条光曲线；
- 曲线随时间不断改变形态；
- 历史时间切片自然形成残影、丝带与网纱。

这比“蛇形线条拖着形状走”更接近屏幕艺术家的动态作画方式。

### 2. 重写 `drawCoherentFlowCreation(...)`

新的 `drawCoherentFlowCreation(...)` 不再直接调用 V120 的四种即兴几何原型，
而是统一进入：

- `drawScreensaverArtistBrush(...)`

此方法负责生成：

- 当前光曲线；
- 多个历史光曲线；
- 平行笔触通道；
- 当前高光脊线；
- 当前薄膜面；
- 笔尖高光。

### 3. 新增 `buildScreensaverControlCurve(...)`

该方法构造一条由运动控制点生成的光曲线。

控制点不是固定点，而是基于当前笔触源轨迹加上：

- 法向运动；
- 切向运动；
- 不同相位；
- 不同速度；
- 轻微呼吸系数。

这样主体更像一条有生命的光曲线，而不是机械线条。

### 4. 新增曲线插值和偏移工具

新增：

- `sampleSourceStrokeByU(...)`
- `catmullRomPoint(...)`
- `offsetCurveByNormal(...)`
- `drawScreensaverBrushTip(...)`

用途：

- 对源笔触进行平滑采样；
- 使用 Catmull-Rom 生成连续曲线串；
- 用法向偏移生成平行通道；
- 在当前笔尖生成更有神采的高光。

### 5. 用历史时间切片替代“单独形状跟随”

V121 中，丝带、网纱和残影不是另一个独立形状，
而是由同一组控制点在不同历史时间的曲线切片形成。

这样可以避免：

- 一条线和一个形状分离；
- 形状紧随线条漂移；
- 看起来像贪吃蛇拖着尾巴。

---

## 三、预期效果

V121 的目标是：

- 更像 Windows 屏保式的动态光曲线；
- 更像同一组控制点在持续作画；
- 更少“蛇形表演”；
- 更少“线条/形状分离”；
- 以历史残影自然形成艺术丝带与光纱。

---

## 四、同步更新页面文案

已同步更新：

- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`

版本文案升级为 V121。
