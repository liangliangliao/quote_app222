# VOICE_ALARM_V18_STT_ACCURACY_AND_CORRECTION_ROOT_CAUSE_FIX

## 本轮背景

用户反馈（V17 之后仍然存在）：

1. 语音转文字体验仍然不好；
2. 经常漏掉用户说的话；
3. "语音修正"功能感觉没有起作用。

V1～V17 历次修复主要在调参数（VAD 阈值、静音时长、pre-roll 长度、队列容量等），
但本轮逐行复核 `VoiceAlarmActivity.kt`（约 2100 行，闹钟语音识别/修正的全部逻辑
所在文件）后发现，真正的根因是**架构/逻辑层面的缺陷**，调参数无法解决，必须改代码。

## 缺陷 1（核心根因）：跨语句合并算法会整体吃掉之前说的话

`mergeLiveDictationText()` 中"两段文本相似度 ≥ 72% 就整体替换""一段文本是另一段的
子串就丢弃较短的一段"这套规则，本来是为**同一句话内的局部修订**设计的（例如原生
Android `SpeechRecognizer` 反复用 `onPartialResults` 重新转写同一句正在说的话）。

但旧代码把这套规则无差别套用在"**整个已累计的多句缓冲区**"和"**云端识别（微软/
讯飞）每次返回的全新独立语句**"之间。云端识别每次回调都是一句新内容，只要新句子
和已经缓冲的文字字符重合度凑巧≥72%（中文很容易出现，比如都含"今天""我""可以"
"了"等常用字），旧内容就会被整段替换掉、或者新内容因为是旧内容的子串而被直接丢弃。

**这是"经常漏掉用户所说的话"的头号根因**，而且会被用户误判为"语音修正"在捣乱
（实际上是合并逻辑在删内容，不是真的识别错误）。

### 修复方式

重写 `appendSpeechChunk()`：引入 `lastAppendedChunkStart` 边界指针，把缓冲区切成
"已落定前缀"（之前已经 finalize 的语句）和"当前未锁定尾部"（正在说的这一句）。

- 相似度/子串判断**只允许**作用于"当前未锁定尾部"，不再有机会碰到之前已经说完
  的内容；
- 一句话被标记为 `finalChunk = true`（云端识别每次回调、原生识别的 `onResults`）
  后立即"上锁"，下一句新内容只能追加，绝不可能整体替换或丢弃已锁定的旧内容；
- 新增的 `mergeSpeechText()` 同步去掉了原来"整体包含即丢弃一方"的危险分支，
  只保留"拼接处字面重叠去重"这一种安全操作（比如两段衔接处偶然出现相同的字）。

## 缺陷 2：极短识别结果被无条件当噪声丢弃

`isLikelyNoise()` 原逻辑：

- 识别结果 ≤1 个字符 → 无条件当噪声丢弃；
- 识别结果 ≤2 个字符且不是停止/稍后提醒/唤醒词 → 当噪声丢弃。

"好的""可以""知道""没事""谢谢""好""对""走"这类极常见的中文短回答，在还没进入
缓冲合并逻辑之前，就已经在文本层被直接吞掉，界面不会有任何提示。这与 V17 文档
自己声明的目标（"降低'好的、可以、确定、不要'等短句被丢弃的概率"）实际矛盾——
V17 那次只修了**录音阶段**的最短音频时长过滤，没有发现**文本阶段**这一道更晚生效、
门槛更高的噪声过滤器。

### 修复方式

`isLikelyNoise()` 改为只过滤真正的语气词（嗯/啊/呃/哦/喂/哈/唉/呀 等的精确匹配），
不再按字符长度一刀切丢弃。

## 缺陷 3：每句话之间反复重建 AudioRecord，制造录音空窗

`recordPcmChunk()`（微软识别用）和 `recognizeWithIflytekStreaming()`（讯飞识别用）
此前**每次被调用都会 new 一个新的 `AudioRecord`，结束时又 `stop()+release()`**。
即每说完一句话（静音触发分段），话筒就被销毁重建一次。多数机型上这个硬件级别的
重建会有几十到数百毫秒的间隙——如果用户停顿很短就接着说下一句，开头的字正好
落在这个"话筒正在重建"的空窗里，识别引擎根本收不到这段音频，自然就是"漏字"。

### 修复方式

新增 `obtainContinuousAudioRecord()` / `releaseContinuousAudioRecord()`，维护一个
跨多个语句分段**持续存活**的共享 `AudioRecord`：

- 只有当需要的音频源在"正常识别（VOICE_RECOGNITION）"和"闹钟/AI播报期间的回声
  抑制（VOICE_COMMUNICATION）"之间真正切换时才重建（这种切换本身就比较少发生，
  且发生在明确的播报开始/结束边界上）；
- 同一种模式下连续说多句话，全程复用同一个话筒实例，不再有逐句重建的空窗；
- 在 `stopCloudStt()` / `onDestroy()` 中统一释放，避免遗漏导致话筒常驻占用。

**需要真机验证**：切换识别引擎（微软⇄讯飞）或闹钟生命周期变化时，旧的采集线程
可能仍阻塞在 `read()` 调用中，与新线程释放共享 `AudioRecord` 之间存在一个很窄的
跨线程时序窗口；当前已用 try/catch 包裹，预期最坏情况是捕获到一次异常并自动重建，
但建议重点测试"语音助手开启 → 中途切换识别引擎 → 立即说话"这条路径。

## 缺陷 4："语音修正"对默认识别引擎（微软）完全不存在，但文案没有区分

页面默认 `_sttProvider = 'microsoft'`。微软识别走的是 REST 一次性识别（每段录音
结束后整体提交一次），**没有讯飞 `dwa=wpgs` 那种流式动态纠错协议**——这一点 V16/
V17 文档其实已经承认，但闹钟设置页的说明文字（"全自动实时听写 + 动态修正 + ……"）
并未做区分，用户只要没手动切到"讯飞识别"，"语音修正"四个字对他来说就是空话，
体验上自然是"修正没起作用"。

### 修复方式

`lib/voice_alarm/voice_alarm_page.dart` 文案拆分为两部分：

- 通用说明去掉"动态修正"字样（避免暗示微软也有该能力）；
- 新增一行根据当前选择的识别引擎动态变化的提示：选讯飞时说明"讯飞支持真正的
  动态修正"；选微软时明确提示"微软目前不支持实时动态修正，如需体验请切换到
  讯飞识别"。

真正让微软也具备同等动态修正能力，需要把当前的 REST 短语音识别换成 Azure Speech
SDK 的连续流式识别（WebSocket，解析 partial/stable/final 事件），这是一个独立的
更大改造，本轮未实施，仅做文案纠正以避免用户误判。

## 修改文件

- `android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt`
- `lib/voice_alarm/voice_alarm_page.dart`

## 静态检查

- Kotlin / Dart 括号数量检查通过（圆括号、花括号、方括号均配平）；
- AndroidManifest.xml 解析通过；
- 全文搜索确认无遗留的已删除字段（`lastAppendedChunkRaw`）引用；
- zip 根目录无多级目录嵌套；
- 受限于沙箱环境无 Android SDK / Gradle / 网络访问 Google Maven，本轮**未能执行
  真实 Gradle 编译**，以上为源码静态检查，建议导入 Android Studio 后先跑一次
  `:app:compileDebugKotlin` 确认零警告再上机测试。

## 仍需真机验证的点

1. 连续说多句话（中间正常停顿）时，是否还会出现"前面说的话被吃掉"的情况；
2. "好的""可以""知道了"等短句是否能正常被识别并送入 AI；
3. 两句话之间接话很快时，开头字是否还会丢；
4. 闹钟/AI 播报期间打断说话，再到播报结束后连续对话，识别引擎切换路径下是否稳定；
5. 讯飞 `pgs=rpl` 动态修正在真实网络环境下的触发频率与"讯飞已动态修正"提示是否
   符合预期。
