# Action Tree More Paths Update

本次更新：
- 行动拆解页新增“AI 补充更多实现路径”
- 新增 `concept_engine.action_tree.more_paths` / `.fast_retry` 日志模块
- 对步骤增加更稳定的 `importance` / `linkedCabinType` 自动推断
- 方向展开与自定义步骤扩展都会自动补关键训练舱标签
- 本地 fallback 也支持替代路径追加
