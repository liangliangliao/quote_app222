# 语音闹钟非实时录音提交与状态修复

## 修复点

1. 修复“说完 5 秒没有自动提交”的问题：
   - 非实时录音现在不再只依赖严格 VAD 阈值；增加 RMS + peak 的软语音检测。
   - 只要检测到软语音并静音超过配置时长，就会自动结束当前段并加入识别队列。

2. 修复“说了很多话，手动提交提示未检测到有效声音”的问题：
   - 录音线程现在始终缓存当前段原始 PCM。
   - 即使严格 VAD 未确认 speech，手动提交也会提交当前缓存音频，避免 VAD 误判导致音频被丢弃。

3. 修复“手动提交后没有状态反馈/可重复点”的问题：
   - 增加提交中状态。
   - 提交中按钮文字变为“提交中…”。
   - 提交中禁用手动按钮，防止重复提交同一段录音。
   - 页面明确显示：手动提交中、已入队、提交中请求服务商、未返回有效文字等状态。

4. 修复返回结果展示语义：
   - 非实时识别返回后显示为“非实时识别结果”，不再显示“实时草稿”。

## 修改文件

- `android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt`
