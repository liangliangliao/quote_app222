# 《意志之镜》V4.0 集成说明

本实现依据《离线知识库驱动的“表象—意志—性格—自我一致人生”产品架构深度研究报告》，在“发现之旅”中增加独立的“意志之镜 · 自我一致人生”入口，并保留已有“向己 · 智能目标导师”和“向己 · 未来军师”模块。

## 用户旅程

1. **当前欲望**：只记录表象目标、领域和当前欲望强度，不提前替用户解释。
2. **Why Tree**：允许一问多答和并行分支；语义循环、分支汇聚或信息增益降低后，系统提示 `Why → Where Else`。
3. **跨人生验证**：通过 Real-Me、实际行动、受阻/痛苦、真实代价和主动反证建立人生证据。
4. **探针校准**：提供无人知晓、去赞美/比较、反对、反对消失、功能替代、满足残差和 MPS；“反对”和“反对消失”必须成对解释。
5. **获得性格**：用支持、反证、情境限制三栏证据矩阵维护可修订候选。没有主动反证检索，不发布经验性格结论。
6. **现实实验**：创建七日低风险实验，逐日记录能量、最像自己、持续性和满足感，以现实反馈修正模型。

## 数据与推理边界

- 公共理论证据存于版本化 JSONL，启动时校验 SHA-256 并编译为独立、只读 SQLite KB。
- 私密人生数据存于独立 SQLite Vault；所有用户文字和推理载荷在进入 SQLite 前使用 Android Keystore 中的 AES-256-GCM 密钥加密。
- 默认仅本地保存；云同步和 AI 推理均不默认开启。单次 AI 推理需用户明确同意，且只发送已关联证据与短 KB 摘要。
- AI 输出必须引用输入中的证据 ID，同时包含反证/限制、不确定性和下一步现实检验；不输出人格百分比。
- “意志”是哲学工作框架，不宣称测量物自身、本质、唯一真实需要或童年根因；本功能不是诊断或治疗。

## 离线 KB

- 22 条结构化记录，覆盖叔本华 §18、§22、§29、§34、§38、§53–55，Tal L12/L13/L22，Sheldon & Elliot self-concordance 与 VIA 边界。
- `original_text`、`zh_translation`、`summary`、`product_rule`、`misuse_boundary` 分字段保存。
- 证据等级为 A、A-candidate、B、C；A-candidate 继续保留逐段核验/授权门，不伪装成已完成的一手证据。
- 检索同时返回支持、反证和边界三组材料；当前版本明确采用词法、标签、权威等级、来源忠实度与模块匹配的透明混合排序，不虚构向量模型。

## 自动化验证

专用 `Will Mirror V4 CI` 固定使用 Flutter 3.35.3，执行：

- KB manifest 哈希与 22 条记录校验；
- `lib/will_mirror` 与 `test/will_mirror` 静态分析；
- Deep Why 边界、成对探针、反证门和置信带测试；
- KB 编译、SQLite integrity check 与支持/反证/边界三路检索测试；
- Vault 加密落库、默认无同步、明文导出警告和密码学删除测试；
- “发现之旅”入口和五个长期入口组件测试。

仓库原有 `Build Android APK` 工作流继续承担完整 Release APK 编译和 Kotlin/Flutter 集成验证。

## 正式发布前仍需产品/法务完成

- A-candidate 讲座材料的逐段时间码核验、授权与权利状态确认；
- 面向真实用户的误用率、反证召回、退出/删除成功率及安全文案校准；
- 真机验证 Android Keystore 的备份恢复、系统升级、锁屏变化和密钥失效路径；
- 如未来引入本地 embedding，必须记录模型、维度和索引版本，并保留当前词法检索回退。
