# PATCH V63: 活体光弦网 / 放大舞台 / 持续流动修复

针对 V62 真机反馈“主体一动不动、主体过小”的问题，本版只重写主体阶段的光弦网绘制逻辑，释放与余韵阶段保持不变。

## 关键原因

- V62 使用 `minDim` 做主体尺度，在竖屏设备上等于屏幕宽度，主体天然偏小。
- V62 对所有流线点做了屏幕边界 `clamp`，远端大量顶点被钉在左侧同一条竖线上，形成静态硬边，同时进一步压缩了主体宽度。
- V62 的 reveal 在前段完成后，后续只剩很弱的低频微扰，视觉上像一张停住的小线网。

## 修复内容

1. 去除流线顶点的硬边界 `clamp`，离屏部分交给 OpenGL 自然裁切，避免左侧竖直硬边。
2. 使用屏幕宽高分别定标：横向 span 由屏幕宽度控制，扇面高度由屏幕高度控制。
3. 把线束生成改为从喉口/笔尖向外“回画”历史光弦，喉口始终可见，不再出现远端线场和能量核断开的情况。
4. 每帧持续驱动喉口位置、旋涡强度、展开角、曲线控制点和细微手绘扰动。
5. 提高主体线条、高光和极淡膜面的可见度，同时保持线条主导、膜面辅助。
6. 入口文案同步更新为 V63。

## 验证

已对核心 Java 文件执行语法级编译检查通过。完整 Gradle 构建仍受源码包缺少 `android/gradle/wrapper/gradle-wrapper.jar` 限制。
