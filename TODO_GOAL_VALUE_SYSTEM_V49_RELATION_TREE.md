# V49 Microsoft To Do 目标实践系统：今日旅程关系树

## 改造目标

根据反馈，今日旅程中的每个子目标、子步骤不应孤立呈现，而应让用户一眼看懂：

- 这个行动属于哪个父目标；
- 它是否接续某个父步骤；
- “缩小一步”和“复盘生成下一步”生成的新行动从哪里来；
- 当前行动位于整条实践链条中的第几层。

## 本次新增能力

1. 今日旅程页由“平铺行动卡”升级为“今日行动关系树”。
2. 每个父目标下面按树状结构展示今日步骤。
3. 每个行动卡显示关系路径：
   - 直属父目标；或
   - 接续父步骤。
4. 点击“缩小一步”后，新步骤自动挂到当前步骤下面。
5. 复盘后生成“明日最小一步”时，新步骤自动挂到被复盘的步骤下面。
6. 目标详情页的行动步骤也改为关系树展示。
7. 写回 Microsoft To Do 的任务正文中加入关系路径：父目标、父步骤、今日行动。

## 数据库变更

`goal_action_steps` 新增字段：

```sql
parent_step_id TEXT DEFAULT ''
step_level INTEGER DEFAULT 1
sort_order INTEGER DEFAULT 0
```

已加入迁移逻辑，旧版本数据库会自动补充字段。

## 修改文件

- `lib/external_data/todo_goal_models.dart`
- `lib/external_data/todo_goal_dao.dart`
- `lib/external_data/todo_goal_pages.dart`

## 设计原则

目标实践系统不再只告诉用户“今天做什么”，而要让用户看到行动之间的来源关系：

```text
父目标
  └─ 父步骤
       └─ 子步骤 / 今日最小行动
```

这样用户能够把微小行动放回目标整体脉络中，避免“零散任务感”，强化“目标 → 行动 → 复盘 → 下一步”的连续过程。
