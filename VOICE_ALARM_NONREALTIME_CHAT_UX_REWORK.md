# VOICE_ALARM_NONREALTIME_CHAT_UX_REWORK

本次针对“非实时语音模式体验仍不像 ChatGPT / Grok / 常见聊天 App 的语音输入”的问题继续重构。

## 关键问题

旧实现虽然支持非实时录音转文字，但仍存在这些体验缺口：

1. 录音状态和识别结果共用同一个文本区，导致“识别结果刚显示，又被录音状态覆盖”。
2. VAD 过严时，自动提交倒计时不明显，用户不知道系统是否听到了自己。
3. 手动按钮没有显示当前录音缓存时长，用户不知道是否可发。
4. 非实时识别结果仍走实时识别的回声/噪声过滤，可能出现“服务商返回了文字但页面没显示”。
5. 服务商返回空文本后，用户只能重新说，不能直接重试上一段录音。

## 本次改造

### 1. 分离“转写结果区”和“录音状态区”

- `transcriptView` 保留用户转写结果和 AI 对话提示。
- 新增 `batchStatusView` 显示非实时录音状态：等待说话、已检测到声音、静音倒计时、提交中、识别完成、可重试。
- 这样录音状态不会覆盖已经返回的转写文本。

### 2. 手动按钮显示录音缓存时长

- 新增 `batchCurrentCachedSeconds`。
- 按钮从“开始说话后可发送”变为“发送当前录音转文字（x.x秒）”。
- 提交中变为“识别中，请稍候…”，且禁用，防止重复提交。

### 3. 更宽松的聊天式候选语音活动检测

- 新增 `hasBatchCandidateVoiceActivity(...)`。
- 它只用于启动状态提示和自动静音倒计时，不决定最终文本可信度。
- 目的：宁可提交一段可能为空的录音，也不要因为本地 VAD 过严导致用户说了却不提交。

### 4. 手动提交优先发送完整原始录音

- 手动提交不再优先使用本地裁剪段，而是优先使用完整缓存音频。
- 自动提交仍可使用裁剪段；若返回空文本，会回退到完整原始录音重试。

### 5. 非实时结果绕过实时回声过滤

- 新增 `onBatchTranscriptionResult(...)`。
- 非实时结果来自用户主动提交的录音文件，不再进入实时 STT 的 speaker echo/noise 分类。
- 避免“服务商已经转写，但页面被当作回声忽略”。

### 6. 增加“重试上一次录音”

- 新增 `batchRetryButton` 和 `batchLastRetryJob`。
- 当服务商没有返回有效文字时，保留本段录音，允许用户直接重试，无需重新说。

## 修改文件

- `android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt`
- `lib/voice_alarm/voice_alarm_page.dart`
