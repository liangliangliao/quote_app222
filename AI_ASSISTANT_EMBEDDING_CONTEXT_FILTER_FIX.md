# AI Assistant Embedding Context Filter Fix

本补丁修复聊天助手在“Eden AI Embedding 语义检索”模式下仍然把无关历史消息发送给聊天模型的问题。

## 问题原因

上一版 embedding 检索使用的相似度阈值较低，短问题例如“今天是星期几？”也可能和旧的长回复产生弱相似度，从而把无关历史拼进请求。

## 修复内容

- 语义检索阈值从 `0.18` 提高到 `0.55`。
- 新增动态阈值：只保留接近最高相似度的历史消息。
- `topK` 从 8 调整为 6，避免一次带入过多历史。
- 如果没有强相关历史，embedding 模式只发送当前用户消息。
- 如果 Eden AI embedding 构造上下文失败，错误仍写入 App 日志表，但不再回退为“拼接全部历史”，而是只发送当前用户消息，避免突然带入大量无关历史。

## 当前两种模式

1. 拼接全部历史：按本地完整历史发送。
2. Eden AI Embedding 语义检索：只发送强相关历史 + 当前用户消息；无强相关历史时只发送当前用户消息。
