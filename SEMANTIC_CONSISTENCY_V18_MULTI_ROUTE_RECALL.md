# Semantic Consistency V18 — 多路召回诊断升级

本次改造针对“单一 Embedding cosine 容易漏召回/误召回”的问题，对语义一致性精判模块做了如下升级：

1. **Fast Embedding 模式升级为“多路召回诊断”**
   - 保留原始 A/B Embedding cosine。
   - 新增分句/片段召回：长文本会切成语义片段，短文本会按句/分号/逗号拆分。
   - 新增概念原型召回：内置“知行不一、认知和行动脱节、行动阻抗、目标无法转化为行为、拖延、完美主义、恐惧失败、反刍/内耗、情绪干扰、目标模糊/动力不足”等核心概念原型。
   - 新增典型表达召回：概念会展开为定义、别名、典型口语表达后参与最高命中计算。
   - 新增关键词/规则召回：例如“想很多/知道/计划 + 做不起来/无法开始/无法落实”会被识别为知行不一/认知行动脱节相关。

2. **修复短文本乱序误判**
   - 不再把“字符集合相同”直接显示成 1.0 词面相似。
   - 新增 edit / bigram / trigram / charSet 四类词面诊断。
   - 对“各行其是 vs 是其行各”这类同字乱序短语触发上限惩罚，避免 embedding 或字面重合误导。

3. **避免 embedding 一票否决**
   - 标准精判/概念关系精判中的 Embedding 诊断改为多路召回诊断，只作为参考，不作为最终结论。
   - 高精度候选精排不再只按 Embedding 排序，而是按 max(embedding_score, local_rule_score) 并结合词序上限惩罚排序。

4. **UI 增强**
   - 结果页新增“多路诊断分”“最高召回向量”“本地概念/规则”“命中通道”“诊断码”。
   - 新增“查看多路召回诊断”展开项，显示 route_scores、surface_stats、concept_hits、local_rule_hits。
   - 快速示例新增“概念 vs 口语表现”和“短文本同字乱序”。

5. **配置页文案更新**
   - 将“Embedding 粗筛配置”更新为“Embedding / 多路召回配置”。
   - 阈值说明强调：多路诊断用于减少漏召回/误召回，不代表严格事实等价。
