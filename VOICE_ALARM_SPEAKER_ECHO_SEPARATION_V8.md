# 语音闹钟：用户语音 / App 播报 / 环境噪声分离修复 V8

## 背景

v6 之后为避免连续讲话丢失，云 STT 改成持续分段录音。但新的副作用是：AI 回复或闹钟提示通过扬声器播报时，也可能被麦克风收进去，再被分段 STT 识别成“用户说话”，从而形成 AI 自问自答、打断播报、错误暂存等问题。

单麦克风场景下无法做到 100% 物理意义上的“绝对区分”，但因为 App 明确知道自己何时在播报、播报了什么文本，所以可以通过“播放状态机 + 系统回声消除 + 本地 VAD + 文本回声指纹 + 延迟缓冲复核”把误识别率显著降低。

## 本次实现

### 1. 增加播放状态机

新增状态：

- `speaking`：App AI TTS 正在播报；
- `alarmVoicePlaying`：闹钟提示音 / 自定义语音正在播报；
- `appPlaybackStartedAt`：本轮 App 播放开始时间；
- `strongPlaybackGateUntil`：强回声门控窗口。

播报刚开始的 1.8 秒内，非指令短片段默认按回声处理，不再直接进入 AI 请求。

### 2. AI TTS 播报也进入回声门控

之前主要拦截闹钟播报回声，但 AI 自己回答时仍可能被云 STT 听到。

现在 `speak()` 和 `UtteranceProgressListener.onStart()` 都会调用播放门控逻辑，AI TTS 期间识别结果必须先经过回声分类。

### 3. 三类声音分类

新增 `SpeechOrigin`：

- `USER`：用户真人语音；
- `PLAYBACK_ECHO`：App 自己的闹钟或 AI 播报回声；
- `NOISE`：环境噪声、无意义短片段、填充音。

只有 `USER` 才允许：

- 暂停/推迟闹钟重播；
- 写入语音缓冲；
- 发送给 AI。

### 4. 增强文本回声指纹匹配

回声过滤不再只依赖 `contains()` 或少量 bigram，而是增加：

- 子串匹配；
- 6 字窗口匹配；
- LCS 最长公共子序列相似度；
- bigram 重合比例；
- 播报中与非播报中采用不同阈值。

这能处理 ASR 把播报内容识别成“少字、多字、轻微错字、片段化”的情况。

### 5. 系统级回声消除 / 降噪 / 自动增益

云 STT 录音时新增：

- `AcousticEchoCanceler`；
- `NoiseSuppressor`；
- `AutomaticGainControl`；
- 播报期间优先使用 `VOICE_COMMUNICATION` 音源，非播报期使用 `VOICE_RECOGNITION`。

这些能力如果设备支持就启用，不支持则自动降级，不影响录音。

### 6. 讯飞流式 STT 不再把播报前噪声直接推给云端

之前讯飞流式识别一开始就把麦克风 buffer 发送给服务器，因此播报声/房间噪声会被识别成文本。

现在改成：本地 VAD 先判断出现明显语音后，才开始向 WebSocket 发送音频帧。没有检测到有效语音时直接关闭本轮，不发送空噪声。

### 7. 播报期间的识别结果延迟复核

如果播报尚未结束，且识别结果不是关闭/延迟这类紧急指令，则继续暂存，不立刻发给 AI。播报结束后再次执行回声过滤，避免 AI 自己说完后被当成用户追问。

## 修改文件

- `android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt`

## 仍需说明

单麦克风 + 外放扬声器场景无法做到理论上的 100% 声源分离。真正更强的方案是：

1. 设备端 AEC/NS/AGC；
2. App 播放状态机；
3. 已知播报文本的回声指纹过滤；
4. VAD 能量门控；
5. 播报期间延迟复核；
6. 用户明确插话时再进入 AI。

本次已经按这个方向在源码中落地。
