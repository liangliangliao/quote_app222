# V20 环境子模块中文化与按钮可执行修复

## 背景
用户反馈“环境”子模块中“AI 设计 Prime / AI 清理 Anti-Prime”按钮点击后没有明显结果，并且页面出现 Prime、Anti-Prime 等英文术语，不符合最终产品设计方案中“注意力环境设计”应作为可执行训练入口的要求。

## 发现的问题
1. 环境页两个按钮只把文本写入“解决问题”页并切换 Tab，没有直接生成环境训练结果；用户会感知为“点击没反应”。
2. 页面暴露英文术语：Prime、Anti-Prime、Benefit 问题、AI 设计等，普通用户理解成本较高。
3. 环境页只像数据管理台，缺少“点击即生成、保存、进入详情”的闭环。
4. 旧文案把内部术语当用户术语，没有体现最终方案中的中文产品语言：注意力启动线索、消极启动源、资源发现问题、最小环境改造。

## 本次修复
1. 新增 `_generateAttentionEnvironmentRecord()`，环境页按钮现在会直接调用智能生成服务，保存记录，刷新数据，并打开详情页。
2. 新增两个环境快捷生成动作：
   - `_generateDailyAttentionCue()`：生成今日注意力启动线索。
   - `_generateNegativeCueCleanup()`：生成消极启动源清理方案。
3. 环境页按钮改为：
   - “生成今日注意力启动线索”
   - “生成消极启动源清理方案”
   - “新增关系感谢表达”
4. 增加加载进度条，避免生成过程中无反馈。
5. 环境页核心文案中文化：
   - Prime → 注意力启动线索
   - Anti-Prime → 消极启动源
   - Benefit Finder 问题 → 资源发现问题
   - AI 设计/AI 清理 → 生成/智能生成语义
6. 输出质量提示词增加中文术语约束，避免模型在面向用户的标题和正文中继续输出英文功能名。

## 与产品设计方案对齐
- 环境不是装饰，而是训练外部脚手架。
- 注意力启动线索用于把用户拉回价值和可控行动。
- 消极启动源清理用于减少比较、拖延、自责和无力感。
- 每次只给一个最小环境改造动作，不要求一次性改变全部环境。
- 生成结果进入记录、证据和可复用线索，而不是停留在一次性文字。
