# V24 行为观察复盘多维统计修复

本次修复针对“复盘不能只依赖时间块”的问题。

## 问题

之前复盘页把“时间花在哪里”作为唯一核心视角。当用户没有记录开始/结束时间、没有屏幕使用/日历/番茄钟导入时，复盘页会显示“当前范围还没有可计算时长”，导致用户误以为没有任何可分析数据。

但行为观察模块的价值不应只依赖时间字段。即使没有时长，也可以从行为类别、记录来源、观察层面、情绪、环境等维度看见行为结构。

## 修改

### 1. 新增“行为类别比重”核心卡片

复盘页在时间统计前新增：

- 按类别展示记录次数占比；
- 有时长时同时展示时长占比；
- 无时长时仍可展示类别比重；
- 屏幕使用时间、系统日历等仍使用统一类别规则。

这样用户不记录时间块，也能知道近期行为主要集中在哪些类别。

### 2. 新增“多维观察结构”卡片

增加非时间维度统计：

- 观察层面分布；
- 记录来源分布；
- 情绪出现分布；
- 环境线索分布。

这些数据作为时间统计的补充，不再让复盘页在缺少时长时变成空白。

### 3. 调整无时长提示

当没有可计算时长时，不再只提示“需要记录时间块”，而是说明：

- 上方类别比重仍然有效；
- 时间占比需要时长；
- 可以通过时间块、屏幕使用、日历、番茄钟导入补充时间维度。

## 修改文件

- `lib/behavior_tracking/behavior_tracking_home_page.dart`

## 设计原则

复盘页的优先级调整为：

1. 先看行为类别比重；
2. 有时长时再看时间流向；
3. 用具体行为排行解释“做了什么”；
4. 用多维结构补充“从哪些层面观察到这些行为”。

