# 语音闹钟 V15：全自动实时听写与动态纠错落地

## 背景

用户反馈：不能新增“说完了”按钮，但希望接近讯飞输入法/微信语音转文字的流畅体验。上一版仍存在“可能没说完”式的语义猜测，体验不自然。

## 本次核心改造

1. **从“猜测是否说完”改为“文本稳定 + 静音稳定”自动提交**
   - 不再依赖“因为/但是/我觉得”等语义规则阻塞提交。
   - 系统只判断：最近是否还有新语音、识别文本是否还在变化、是否达到自动提交静音窗口。

2. **新增 Live Dictation 草稿层**
   - partial/final 结果先进入实时听写草稿。
   - UI 显示“正在听写”，而不是把每个片段都当成最终“你：”。
   - 最终只有稳定后的文本会进入 AI。

3. **动态纠错式合并**
   - 对相似的识别结果采用 replacement，而不是 append。
   - 避免出现“同一句话被重复拼接”的问题。
   - 对明显连续的新句段仍做拼接。

4. **讯飞流式识别参数增强**
   - 保留 `dwa=wpgs` 动态修正。
   - 增加 `ptt=1` 自动标点。
   - 增加 `vad_eos`，与用户设置的自动提交静音窗口同步。

5. **热词增强**
   - Android 原生识别加入偏置词：讯飞、微信、DeepSeek、ChatGPT、API、Flutter、源码、编译、报错、语音识别、TTS、AI 等。

6. **设置页改造**
   - 预设改为：极速自动提交、自然自动提交、长句自动提交、慢语速自动提交。
   - 新增“文本稳定等待”参数。
   - 删除“软语义等待”作为主要决策条件。
   - 说明文案强调：自动提交，不需要“说完了”按钮。

## 主要修改文件

- `android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt`
- `lib/voice_alarm/voice_alarm_page.dart`

## 验证

- Kotlin/Dart 括号结构检查通过。
- AndroidManifest.xml 解析通过。
- zip 根目录保持非多级目录结构。

受限于源码包缺少 `android/gradle/wrapper/gradle-wrapper.jar`，当前环境无法离线执行完整 Gradle APK 构建。
