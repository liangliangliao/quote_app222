# PATCH_TOUCH_MYSTIFY_V124_CLEAR_MYSTIFY_STRING

本次继续优化“发现之旅 / 生理赋能 / 触摸”动画主体，重点响应：

- 用户反馈：当前效果不清楚，主体像一团光雾 / 乱网；
- 要求：继续深度分析 Windows Mystify 技术方案并优化当前动画源码。

## 一、问题原因

V122 / V123 已经从“蛇头拖尾”改成“控制点光弦”，但仍然存在几个导致画面不清楚的原因：

1. 历史切片太多；
2. 每个历史切片又有多条侧向 lanes；
3. 旧残影透明度偏高；
4. 控制点沿主轴方向的扰动过大，容易交叉折叠；
5. 当前主光弦不够突出，导致新旧残影混成一团。

Windows Mystify 的关键不是生成一团复杂网，而是：

- 一条或少数几条清晰弯曲光弦；
- 光弦整体不断变形；
- 历史残影短暂保留并快速淡去；
- 当前光弦比旧残影更清楚。

## 二、核心修复

文件：
- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

### 1. 减少光弦数量
将正式模式下的 `strings` 收敛为 1。

目的：
- 避免多个光弦叠加导致主体混乱；
- 先把一条光弦做清楚。

### 2. 减少历史残影数量
将非省电模式 trails 从 30 降到 18。

目的：
- 减少旧残影堆叠；
- 让残影成为“短暂记忆”，而不是糊成一团。

### 3. 减少侧向 lanes
将非省电模式 lanes 从 5 降到 3。

目的：
- 保留一定光带厚度；
- 避免一条曲线扩成过宽光雾。

### 4. 降低旧残影透明度
将旧残影 alpha 由偏高的二次衰减，改为更强的三次衰减：

- 远古残影更快变淡；
- 当前光弦更突出。

### 5. 控制点保持沿主轴次序
在 `buildScreensaverControlCurve(...)` 中：

- 控制点数量从 7 收敛为 6；
- 主轴基准距离增大；
- 主轴方向随机扰动大幅降低；
- 法线方向仍保留弹跳变化；

目的：
- 避免控制点交叉折叠；
- 让曲线更像清晰的 Mystify 光弦，而不是乱成光团。

### 6. 强化当前主光弦
当前光弦额外强化：

- cyan 主体线；
- white core 高光线；
- 更弱的薄光面；

目的：
- 让用户一眼能看清“当前正在运动的光弦”；
- 旧残影只作为背景记忆。

## 三、同步文案

更新：
- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`

入口文案升级为 V124。
