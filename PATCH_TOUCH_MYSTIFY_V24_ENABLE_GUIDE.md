# V24 动态壁纸启用/权限引导修复

本版针对“设置壁纸时如果检测到用户没有开启动态服务壁纸权限，需要引导开启”的问题做了流程修复。

## 重点改动

1. 新增/强化动态壁纸启用状态检测：
   - `applied`：潮汐之间已经成为当前动态壁纸。
   - `registered_not_enabled`：系统已经识别到潮汐之间，但用户尚未在系统页点击“设置壁纸/应用”。
   - `not_registered`：系统尚未识别该动态壁纸服务，提示用户检查安装、后台/自启动/电池限制等系统设置。

2. 动态壁纸应用助手页面现在直接显示：
   - 当前是否启用；
   - 系统是否识别该服务；
   - 动态壁纸列表/预览入口是否可用；
   - 下一步应该点击哪个入口。

3. Flutter 触摸页在打开助手前会先诊断：
   - 如果系统未识别服务，会先弹出引导说明；
   - 再进入动态壁纸应用助手继续处理。

4. AndroidManifest `<queries>` 新增动态壁纸相关 intent，提升 Android 11+ 设备上查询/诊断入口的准确性。

## 说明

Android 没有一个普通运行时权限叫“动态壁纸权限”。第三方 App 不能稳定静默开启动态壁纸服务。真正的“开启”动作必须由用户在系统动态壁纸页面选择本服务并点击“设置壁纸/应用”。本版将此状态检测和引导流程显式化，避免误判为已生效。
