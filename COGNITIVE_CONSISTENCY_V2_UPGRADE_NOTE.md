# 足下一致行动 · 认知失调模块 V2 升级说明

本次升级基于“强迫服从的认知后果 / 认知失调 / 最小充分理由 / 行动反向塑造信念”的产品方案，继续从 MVP 版本升级为更完整的业务闭环。

## 已补齐的关键功能

1. **价值罗盘**
   - 新增价值档案：价值名称、为什么重要、身份方向、最小行动。
   - 生成 1 美元行动时可一键把价值档案带入失调雷达。

2. **认知失调雷达结构化**
   - AI 输出扩展为：我相信/重视什么、我实际做了什么、我如何解释、不一致点、保护性解释、逃避性解释、修复动作。
   - 不再只是普通文本卡片，而是形成价值—行为—解释结构。

3. **行动前抗犹豫 AI**
   - 新增行动前犹豫输入与 AI 引导。
   - 调用统一提示词配置中心中的“场景：行动前抗犹豫引导”。

4. **行动陪伴升级**
   - 支持 30 秒、3 分钟、5 分钟、10 分钟计时。
   - 证据保存时写入真实计时、难度评分、证据分类、sessionId、来源 sourceType/sourceId。

5. **身份证据账本升级**
   - 新增证据分类：启动、勇气、学习、责任、回来、面对不适、复盘。
   - 支持分类筛选、累计分钟、难度、来源展示。

6. **外部奖励降噪**
   - 新增独立页面，识别打卡、积分、监督、惩罚等外部理由。
   - AI 输出低奖励、低压力、仍可启动的行动，并提醒外部理由削弱内在认同的风险。

7. **自我欺骗与合理化校验**
   - 新增独立页面，区分保护自己、承认困难、逃避责任、合理化不行动。
   - 以“不羞辱但诚实”的方式输出替代解释与修复动作。

8. **价值一致性报告归档**
   - 生成报告后保存到本地历史表 `cc_reports`。
   - 报告基于最近 30 条证据，强调真实行动、回来次数、价值靠近，而不是连续打卡。

9. **数据库升级**
   - 新增 `cc_value_profiles`、`cc_reports`。
   - 扩展 `cc_evidence_records`：证据分类、身份方向、奖励来源、来源类型、来源 ID、开始/结束时间。
   - 修复 V1 中 evidence 保存时 sessionId 为空的问题。

10. **与 To Do 目标行动系统打通入口**
    - 在 To Do 今日行动卡片新增“**一致行动**”按钮。
    - 可以把 Todo 今日行动、最低标准、完成问题、过程价值转入“足下一致行动”模块。
    - 保存证据时保留 `sourceType=todo_goal_step` 与 `sourceId=stepId`，为后续双向回写继续预留链路。

## AI 提示词配置

所有新增 AI 能力仍复用并调用设置页中的：

- 全局价值层 Prompt
- 场景：目标转化为 1 美元行动
- 场景：认知失调分析
- 场景：行动前抗犹豫引导
- 场景：行动后自我解释
- 场景：外部奖励降噪
- 场景：自我欺骗与合理化识别
- 场景：价值一致性报告
- 输出格式：通用 JSON 结构

路径：设置页 → AI 提示词配置中心 → 足下一致行动 · 认知失调。

## 校验说明

当前运行环境没有 flutter/dart 命令，无法执行真实编译校验。本次已完成静态代码改造与打包，建议在本地执行：

```bash
flutter pub get
flutter analyze
flutter run
```
