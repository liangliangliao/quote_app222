# PATCH_TOUCH_MYSTIFY_V162_REFERENCE_HEAD_FAMILY_MORPH

本次继续主动对比标准参考视频，对 V161 进行深度修正。

## 一、主动对比结论

V161 已经从早期的“固定主体/爬行动物/乱线团”推进到了“单一 3D 控制点光弦”。
但它和标准参考视频仍然存在一个主要重大差距：

- V161 更像一条数学控制点曲线在轻微弯曲；
- 标准参考视频更像一段光迹在时间中持续成熟：尾部保留刚才的旧形态并逐渐退隐，头部继续试探并生成下一种形态。

也就是说，V161 有“控制点光弦”，但还缺少参考视频中的“头部继续创造、尾部保留旧笔法”的艺术创作过程。

## 二、V162 的核心修复

主要修改文件：

- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

### 1. 新增 V162 主入口

新增并启用：

- `drawReferenceVideoTimePainterV162(...)`

当前主渲染路径已从 V161 切换到 V162。

### 2. 引入“尾部旧形态 + 头部新形态”的连续变形

新增：

- `v162Family(...)`
- `v162ControlPoint(...)`
- `v162ControlPointForFamily(...)`

V162 不再让整条光弦只是一种数学曲线，而是在同一条光弦内部进行渐变：

- 尾部保持 familyA；
- 头部逐渐进入 familyB；
- 中段通过平滑插值过渡。

这样更接近参考视频中“正在画出来的头部继续创造，先画过的尾部慢慢消失”的效果。

### 3. 支持 6 类参考视频式光形家族

V162 的控制点家族包括：

1. 极简长弧 / 书写丝带
2. 叶片 / 场线扇面
3. 宽薄翼面
4. 钩形 / 花瓣回扫
5. 环形网纱 / 半闭合空间弧
6. 场线长束 / 羽化波束

这些不是同时堆叠出现，而是在同一条控制点光弦的头部与尾部之间连续变形。

### 4. 继续保留单一主体和极淡时间切片

V162 仍然坚持：

- 单一创作中心；
- 单一主光弦；
- 黑底大留白；
- 极淡同源回声；
- 克制白色芯线；
- 不生成多个独立主体；
- 不恢复乱线团和复制分身。

### 5. 附加纹理只在合适家族中出现

新增：

- `v162WideFamilyAmount(...)`
- `v162FilamentAmount(...)`
- `drawV162Filaments(...)`

只有叶片、场线、环纱一类形态成熟时才出现极少量内部纹理，避免再次变成“两侧乱条纹”。

## 三、同步更新

已同步更新：

- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`

版本文案升级为 V162。
