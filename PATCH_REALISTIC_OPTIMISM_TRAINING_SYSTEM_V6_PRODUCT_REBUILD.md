# PATCH_REALISTIC_OPTIMISM_TRAINING_SYSTEM_V6_PRODUCT_REBUILD

## 背景
用户反馈：当前模块实际使用体验与最终版产品设计方案“天差地别”。经复核，V5 虽已补齐入口、表、Prompt 配置和部分复盘字段，但核心体验仍偏“通用 AI 输入框 + 结果卡片”，没有真正按最终方案形成产品化训练流程。

## V6 核心改动

### 1. 新增产品级引导式训练页
新增文件：

- `lib/realistic_optimism_training/realistic_optimism_training_flow_page.dart`

该页面不再让用户只面对一个大文本框，而是按不同训练入口收集结构化信息：

- 真实事件
- 最强情绪
- 自动解释/脑中最强一句话
- 目标/任务
- 最大障碍/拖延理由
- 失败预测/最坏想象
- 具体感恩/品味对象
- 想感谢的人
- 环境触发源/Anti-Prime

生成时会把这些字段组合为结构化输入，并附加每个场景的最终方案验收要求，确保 AI 不是泛泛安慰，而是按照产品设计方案输出完整闭环。

### 2. 首页新增 V6 产品级重构提示卡
首页“训练仪表盘”新增：

- `V6 产品级重构：从“生成卡片”改为“真实训练流程”`

明确说明之前版本的问题，并给出“开始”入口。

### 3. 10 个独立训练入口改为进入引导式流程
原来点击模块卡片只是填充模板并切换到文本框。V6 改为直接进入 `RotGuidedTrainingFlowPage`。

覆盖入口包括：

- 事件强度分级
- 今日事件重构
- 解释风格雷达
- Fault/Benefit 双镜头
- 允许自己为人
- 失败免疫实验室
- 可控失败挑战
- 过程模拟行动器
- 注意力启动墙
- Anti-Prime 清理
- 感恩与品味
- 身份沉淀
- 幸福基线周报

### 4. 工作台保留旧快速输入，但明确推荐引导式训练
“事件重构”Tab 中新增提示和按钮：

- 推荐使用“引导式训练流程”
- 打开引导式训练流程

这样兼容老入口，但把主体验引导到最终设计方案要求的产品流程。

### 5. 引导页内置最终方案闭环检查
每个场景页显示：

- 强度分级
- 情绪允许
- 事实-解释分离
- 解释风格雷达
- Fault / Benefit 双镜头
- 可控点
- 过程行动
- 证据沉淀
- 感恩 / Prime / 身份

同时按当前场景解释重点。

### 6. 引导页内置输出验收标准
每次生成前明确展示：

- 必须承认情绪，不直接鸡汤安慰
- 必须区分事实和解释
- 必须指出 Fault Finder 叙事如何让人无力
- Benefit Finder 必须承认痛苦，不得说“一切都是最好的安排”
- 必须给出 5 分钟内能开始的行动
- 必须生成身份层证据，基于实际行动而不是空泛夸奖

## 主要修改文件

- `lib/realistic_optimism_training/realistic_optimism_training_home_page.dart`
- `lib/realistic_optimism_training/realistic_optimism_training_flow_page.dart`

## 重要说明
本轮不是继续堆表，而是修正产品体验方向：让用户从“填一个大文本框”转向“按现实主义乐观训练闭环逐步输入与生成”。

当前环境无 `dart` / `flutter` 命令，未能执行真实编译；已做源码级括号、引用、导入和基础结构检查。
