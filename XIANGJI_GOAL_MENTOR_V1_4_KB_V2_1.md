# 向己智能目标导师 V1.4 / KB V2.1 落地说明

## 1. 本次基线

- 产品基线：V1.4.0
- 知识基线：V2.1.0
- 默认知识层：`mentor_goal_core_v21`
- 导师：10 位
- L1 默认主要主题：40 个（每位 4 个）
- L2 折叠次级主题：30 个（每位 3 个）
- L3 深层模块：160 个（每位 16 个）
- L4 目标核心依据：350 条（每主题不少于 3 条）
- 每位导师：6 阶段旅程、8 步目标设定、6 个生活实践、6 条诊断路由、4 个案例
- 简练辅助画像：10 份，全部 `default_hidden=true`

应用启动时会对上述数量、默认层和完整性做 fail-closed 校验；任一发布资产缺失时停止加载，不用模型通用记忆补写。

## 2. 与 PR #148 原实现的主要差异

| 项目 | PR #148 原状态 | 本次 V1.4 落地 |
|---|---|---|
| 发现之旅默认入口 | 未来军师问题/战役工作台 | 智能目标导师；未来军师保留为深度战略入口 |
| 主产品闭环 | 问题、战役、行动、认识审计 | 一位导师、一个目标、一个行动、一个提醒、一次校准 |
| 导师知识 | 通用路由/旧知识资产 | 10 位 V2.1 目标核心导师 |
| 导师选择 | 未进入目标主链路 | 创建前明确选择、数据库持久化、保留选择历史 |
| 导师作用 | 无 V1.4 目标路径 | 实质改变价值判断、8 步设定、每日实践和校准 |
| 知识展开 | 未来军师知识中心 | L0 档案、L1 默认展开、L2/L3/L4 按需展开 |
| 简练画像 | 无目标导师页面 | 明确点击后才显示，并标注“知识库综合、非作者原话” |
| 私人书籍 | 未来军师 Provider 生命周期 | 增加本地解析、严格问书、逐书授权、哈希一致和删除传播 |
| 提醒 | 无目标导师定时任务 | 每日最多一次、安静时段、暂停抑制、幂等领取、点击直达 |
| 发布质量 | 未来军师 26 项门禁 | 增加目标导师、KB 完整性、导师差异、隐私事件与 DAO 门禁 |

## 3. 已落地的 P0 能力

### 目标与状态

- 一句话入口、火种卡、用户原话、目标版本历史。
- SQLite 唯一索引保证同一时间最多一个主目标。
- `mist → spark → anchored → active → feedback → calibrating → paused/reselecting/completed/archived` 状态机和非法转换拦截。
- 每次只生成一个可控行动，包含触发点、最低完成、证据规则、可控性理由和更小版本。
- 五类行动反馈、八类成长证据、证据草案确认。
- 四层校准和五种结果；重选保留高层价值。

### 导师与知识

- 创建目标前明确选择导师；选择不是人格标签。
- 当前导师写入目标、导师会话和选择历史；主动更换后重算当前行动，并清除旧导师的 8 步进度。
- 8 步目标设定逐题呈现、逐步保存、可中断恢复。
- 五种缩放：核心、结构、情境、行动、依据。
- 引用只显示短摘录和可回定位位置；知识库应用不冒充作者原话。
- 覆盖不足或资产完整性不合格时停止确定性输出。

### 提醒、文件和 AI

- 目标守护提醒默认关闭；开启后每日最多一次，支持安静时段、失败重试和去重。
- 暂停、重选和归档不会继续发送行动催促；长期沉默使用重连文案，不显示失败天数。
- 私人 PDF、EPUB、DOCX、TXT、Markdown、AZW3 在本机解析为短片段。
- 严格问书只使用本轮允许的书籍和命中片段。
- OpenAI 文件/向量库镜像必须逐书授权；本地 `book_id` 与远端 ID 分离，内容哈希不一致时禁止检索。
- 删除先停止本地检索，再传播到远端；失败显示“处理中”并保留幂等重试记录。
- 原 PR #148 的多 Provider 能力、问题/战役/行动验算和个人战史没有删除，可从“深度问题与战略空间”进入。

### 隐私、安全和发布治理

- 高风险自伤、他伤、控制/家暴和危险性自我忽视会停止普通目标指导。
- 个人案例、目标原话和 8 步回答默认仅存本机，不公开展示，也不成为 AI 永久标签。
- 分析事件只允许登记事件名和枚举/ID/计数；记录器自动丢弃包含 `text`、`input`、`prompt`、`content`、`excerpt`、`answer` 的属性。
- 内置灰度开关覆盖目标核心层、深层导师、简练层、远端镜像和未来军师桥接。
- “本机发布诊断”只显示知识门槛、聚合事件、灰度状态和删除重试，不显示用户原话。
- 数据导出包含目标、版本、状态、行动、证据、导师选择/进度、审计和隐私安全事件；清除使用 SQLite `secure_delete` 并移除提醒和私人文件。

## 4. 开放决策的本次取值

| 决策 | V1.4 取值 |
|---|---|
| O-001 首发平台 | 当前正式交付 Android APK；Flutter 页面保持 iOS 可移植 |
| O-002 “本地”含义 | 用户设备上的只读知识资产和 SQLite 私人数据 |
| O-003 首发 AI 提供商 | 目标私人书库首发使用 OpenAI 官方文件/向量库接口；未来军师保留多 Provider 适配 |
| O-004 账户模式 | 个人本机账户；设备系统解锁作为身份边界，不开放家庭/团队共享 |
| O-005 提醒渠道 | 应用内系统通知 |
| O-006 解析责任 | 产品内置解析；不把整本私人书籍交给外部解析服务 |
| O-007 纯本地模型 | 保留统一适配边界；本版本的确定性导师内容可完全离线读取，生成模型仍按严格来源模式配置 |

路线图中的本地小模型、更多提醒渠道、多语言、团队共享和长期目标组合属于 P1/P2 候选，不是 V1.4 发布门槛。

## 5. 文档冲突处理

`07_knowledge_retrieval_integration.md` 中较早的 V1.4 段落仍提到 `deep_mentor_v2` 默认层；V2.1 发布基线、ADR 和 `default_layer` 字段均明确 `mentor_goal_core_v21` 为默认层。因此本实现按最新冻结项执行：

1. `mentor_goal_core_v21` 默认；
2. `deep_mentor_v2` 折叠支撑；
3. 旧知识树兼容；
4. 简练层默认隐藏。

## 6. 自动化发布门槛

GitHub Actions 在原有知行树和未来军师测试之外，新增：

```bash
flutter test test/xiangji_goal_mentor
```

覆盖：

- V2.1 的 10 / 40 / 30 / 350 完整性；
- 每位导师的 16 / 8 / 6 / 6 / 4 深层结构；
- 十位导师在同一问题上的判断和行动实质差异；
- 固定问题路由、安全闸门和对抗指令；
- 单主目标、提醒幂等、删除/审计、导师持久化和 8 步进度；
- 分析事件隐私字段过滤和灰度开关；
- Android release APK 构建。
