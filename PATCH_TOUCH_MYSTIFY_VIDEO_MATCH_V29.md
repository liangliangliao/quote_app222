# PATCH Touch Mystify V29 — Anti-Repeat Life Flow

本补丁继续优化「发现之旅 / 生理赋能 / 触摸」的 Windows Mystify 风格动态壁纸。

## 目标

V28 已加入艺术导演式母型轮换，但用户反馈：长期观看时仍能感到部分整体形状雷同、艺术变化不够丰富、少数点线有散乱感。V29 的目标是让生成系统更像“持续变化的艺术生命体”，而不是固定规则的线条随机器。

## Android 原生动态壁纸

文件：`android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

渲染线程名称升级为：

- `IntimacyMystifyGLRendererV29AntiRepeatLife`

新增核心机制：

1. **母型冷却记忆**
   - 最近出现过的形态母型会自动降权。
   - 不绝对禁止重复，但显著降低短时间内重复出现同一轮廓的概率。

2. **低差异黄金采样**
   - 出生点不再只依赖普通随机与热区采样。
   - 引入黄金比例 / 低差异采样，让光迹长期覆盖屏幕时更均匀、更自然。

3. **非周期艺术天气**
   - 使用多个不同频率的相位缓慢改变母型偏好。
   - 让动画长期播放时不容易显露固定循环节奏。

4. **新母型**
   - `8`：水母触须 / 水草光丝
   - `9`：星云涡旋 / 烟雾卷弧
   - `10`：折纸光片 / 翻折薄面
   - `11`：星座碎链 / 节点线框

5. **深层变体参数**
   - `variant`
   - `flowA`
   - `flowB`
   - `flowC`
   - `asymmetry`

这些参数不只是改变位置和颜色，也会影响线条内部呼吸、微涡流、纤维偏移和点线附着方式。

## Flutter 页面预览

文件：`lib/pages/touch_mystify_wallpaper_page.dart`

新增同步预览能力：

- `_chooseArtMode(...)`：预览端也启用反重复模式选择。
- 模式从 10 类扩展到 14 类。
- 新增水母触须、星云涡旋、折纸光片、星座碎链。
- 修复 V28 预览中潜在的 `d8 / len / n` 命名错误，避免预览编译问题。

## UI 文案

- 「视觉风格」第一项改为：`反重复生命流`
- 「生理赋能 / 触摸」副标题改为：`触摸 · Mystify 生命光迹：反重复、扇面、花瓣、星云`
- 「发现之旅」提示改为：`Windows Mystify 式反重复生命光迹壁纸`

## 观感预期

V29 不承诺数学意义的无限唯一，但比 V28 更接近：

- 长期观看不容易看出固定循环。
- 近期不会连续出现同一类整体形状。
- 光迹出生位置更均匀，减少局部堆叠。
- 点线不再像散乱颗粒，而是依附在主笔触、节点、折痕、触须、星云回声上。
- 画面更像视频描述中的“花、藤、折扇、薄刃、电子纱、几何片、星云、触须”交替流动。
