# v17 Microsoft To Do 自动同步修复

本版针对“刷新页面没有自动同步远端 Microsoft To Do 数据”做了调整：

1. 进入 Microsoft To Do 首页后，会自动调用 Microsoft Graph 同步数据。
2. To Do 首页右上角刷新按钮不再只是读取本地数据库，而是执行远端同步，然后刷新本地页面。
3. To Do 首页下拉刷新会自动远端同步。
4. 进入具体任务列表页后，会自动同步当前任务列表；智能视图会同步全部任务列表后再刷新本地视图。
5. 任务列表页右上角刷新按钮会自动同步当前列表。
6. 任务列表页下拉刷新会自动同步当前列表。
7. 同步过程中、同步成功、同步失败都会在页面顶部状态卡片和 SnackBar 中提示。

注意：仍需 Microsoft Entra 权限包含 Tasks.Read 和 Tasks.ReadWrite，并在 App 中退出/清除后重新登录，旧 token 才能拿到新权限。
