# PATCH_TOUCH_MYSTIFY_V154_REFERENCE_VIDEO_TIME_PAINTER

本次基于用户提供的标准参考视频继续改造“发现之旅 / 生理赋能 / 触摸”的动态壁纸画笔。

## 一、参考视频观察结论

参考视频的关键不是“一个主体在屏幕上移动”，也不是“先准备好一套模板然后逐个显示”。
它更像黑色画布上的时间绘画：

- 每段视觉都是一段短生命创作短句；
- 位置、方向、颜色、形态会变化；
- 很多形态非常克制，常常只是一段优雅光痕、薄片、扇面、丝带或场线；
- 先画出的内容会逐渐变淡；
- 新内容继续生长，旧内容以短暂残影退隐；
- 变化来自同一段时间中的局部笔痕出生、发展、转向、消失，而不是完整形状直接替换。

## 二、当前源码问题

V153 虽然改成了流场粒子，但仍然存在以下问题：

1. 主体仍容易被看成一条线或一个固定小生物；
2. “线、扇面、场线、丝带、花瓣”等变化不够像同一创作短句中的连续发生；
3. 旧内容消失节奏不够明确；
4. 视觉上仍容易像装饰线附着在主体上，而不是主体正在自由创作。

## 三、V154 改造内容

### 1. 新增参考视频式时间绘画入口
重写：
- `drawFlowFieldArtistCreationV153(...)`

当前主路径改为调用：
- `drawReferenceVideoTimePainterV154(...)`

### 2. 统一创作中心，但不是固定身体
新增：
- `drawV154UnifiedCreativeField(...)`

所有笔痕共享同一个创作中心和同一轮生命周期，避免多主体分裂；但它们不是一条连续身体，而是一组短生命光迹。

### 3. 按出生时间决定局部笔法
新增：
- `v154LocalPoint(...)`
- `v154StyleIndex(...)`
- `v154PointForMode(...)`

同一个主体的旧部分保持旧笔法并逐渐淡出，新头部继续生成新的笔法，从而形成“线条 → 扇面 → 场线 → 丝带 → 花瓣 → 星芒”的连续局部演化。

### 4. 边画边消失
新增：
- `v154Head(...)`
- `v154Tail(...)`

头部继续创造，尾部持续追赶，避免等全部画完后统一消失。

### 5. 只保留极少量同源时间回声
历史痕迹只绘制短笔痕，不绘制完整主体，避免再次出现多个相似主体复制或分身。

## 四、主要修改文件

- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`
- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`
