# PATCH_TOUCH_MYSTIFY_V156_REFERENCE_GAP_FIX

本次基于用户提供的标准参考视频和 V155 实测截图，重新分析当前动画与参考效果之间的主要差距，并对画笔主渲染路径进行重构。

## 一、对比后确认的主要差距

### 1. V155 仍然太像“线团算法”
V155 为了解决 V154 的乱线问题做了收束，但仍然保留了多 stream 机制。多个 stream 叠加后，容易变成一团互相缠绕的线，而不是参考视频中清楚、克制、优雅的一段光短句。

### 2. V155 主体不够像参考视频中的“清晰形体”
参考视频中每一段更像一个明确的光形：丝带、薄翼、钩形、扇面、环形网纱、极简弧线等。V155 仍然把形态建立在一组并行线条上，导致主体边界不清。

### 3. V155 的变化来源仍然不够稳定
V155 中风格变化由 stream 局部点位和附加肋线共同产生，这会造成“看似变化，其实是一组线条互相堆叠”的问题。参考视频中的变化更像：一个清晰主体从无到有展开，然后以时间切片方式淡去。

### 4. V155 留白与节奏不够像参考视频
参考视频有大量黑场留白，主体在不同位置以单个清楚短句出现。V155 仍然容易在一个位置堆出很多线。

## 二、本次采用的技术方向

参考 Windows Mystify 的公开行为描述：一条或多条弯曲光线在黑底移动，持续改变形状、颜色循环，并留下短暂淡去的同形残影；同时借鉴 XScreenSaver / rss-glx 类开源艺术屏保“独立图形生成器持续绘制”的机制。

V156 的实现策略：

1. 保留一个主创作主体；
2. 取消多 stream 乱线团；
3. 使用明确的 motif 形体语法；
4. 让主体从 tail 到 head 的窗口中被绘制；
5. 先画部分通过 tail 追赶自然消失；
6. 每个短句选择一个稳定 motif，而不是每条线各自乱跳；
7. 主体边界由 laneA/laneB 或 fan/loop 场线决定，不再靠大量附着肋线堆出效果。

## 三、主要源码改造

文件：
- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

新增主入口：
- `drawReferenceVideoTimePainterV156(...)`

新增核心方法：
- `v156PhraseLife(...)`
- `v156Head(...)`
- `v156Tail(...)`
- `v156MotifIndex(...)`
- `v156PhraseCenter(...)`
- `drawV156Motif(...)`
- `buildV156Lane(...)`
- `v156Point(...)`
- `drawV156FanField(...)`
- `drawV156LoopMesh(...)`
- `drawV156BrushTip(...)`

当前主路径已从 V155 的 `drawReferenceVideoTimePainterV154(...)` 切换为 `drawReferenceVideoTimePainterV156(...)`。

## 四、V156 预期视觉变化

- 不再出现大团白色乱线；
- 不再多 stream 互相缠绕；
- 主体更清楚；
- 形态更接近参考视频里的丝带、扇形、花瓣、环形网纱、极简光弧；
- 旧内容由 tail 追赶机制逐渐消失；
- 白色核心高光只作为细芯线出现，不再铺满整个主体；
- 仍保留大量黑场留白。

## 五、验证重点

安装后重点观察：

1. 是否还会出现白色乱线团；
2. 是否仍然只是单一线条；
3. 是否能看到主体由局部逐渐展开；
4. 先画部分是否随着 tail 追赶而变淡；
5. 每段短句是否更像参考视频中的清晰艺术光形。
