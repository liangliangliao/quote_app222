# 《意志之镜》V5.0 落地说明

基线：`main@d329f8bbf6facfb1b9b4b2a78c475a5ef8db5c5a`。

本次保留 V4 的 22 条 Evidence KB、三路检索、反证门、独立加密 Vault、逐次 AI 授权和非诊断护栏，重做用户可见的主业务流程。

## 用户可见变化

- 首页从“五个长期入口”改成一个目标/问题输入口和一个主按钮。
- 三步生成三个可落地方案；每个方案展示动作、时长、完成信号、现实产出、原因和理论依据。
- 生成进行中的七日实践；首页只显示“现在做这一件事”。
- 内置 2/5/15 分钟计时器。
- “做了”和“没做成”同样可记录；没做成保留现实限制，并把下一步自动缩成 2 分钟准备动作，不推断没有欲望。
- 四项三选反馈全部由用户填写，不在后台代填持续性或满足感；同一自然日只形成一次七日进度。
- 轻量个性化选择兴趣、引导方式和真实精力，不做人格评分。
- 新增随身助手：默认本地能力目录回答，单条问题可逐次授权 AI。
- 新增流程图、逐功能说明和三个完整七日默认案例。
- 原有 Deep Why、变量探针、Real-Me、行动镜、证据矩阵、实验和 KB 进入“进阶工具”，功能保留。

## 可执行追踪

- `will_mirror_capability_catalog.dart`：所有功能的“是什么、解决什么、输入、步骤、产出、原因和 KB ID”。
- `will_mirror_practice_engine.dart`：三套现实方案生成规则。
- `will_mirror_practice_coordinator.dart`：目标 → Why/阻碍 → 候选 → 七日实验 → 现实观察/行动证据。
- `will_mirror_assistant_service.dart`：本地回答、AI 约束、危机转介和操控性语言禁用规则。
- `example_cases_v5.json`：三例 × 七天可检查测试数据。
- `docs/will_mirror_v5_product_plan.md`：正式产品方案。
- `docs/will_mirror_v5_user_guide.md`：用户操作说明书。

## 数据

V5 偏好、行动方案和助手历史通过既有 `wm_setting.value_enc` 加密保存；目标、证据、候选、实验和观察继续使用 V4 表。导出格式升级为 `5.0` 并包含新增数据。没有新增明文字段或默认云同步。

## 验证

`Will Mirror V5 CI` 继续验证原有 KB 哈希与 22 条记录，并新增：

- 三个默认案例的完整性；
- 每例七天且包含至少一次“没做成”；
- 案例理论 ID 必须存在于 KB；
- 三方案具体性、低压力与依据测试；
- 全功能说明目录完整性；
- 助手操作回答、非诊断和危机路由；
- V5 行动方案加密落库；
- 从输入到做成/没做成记录的完整 Vault 业务闭环；
- 未完成后的真实动作调整、用户自填观察值和同日重复记录拦截；
- 首页、三步方案和发现之旅入口组件测试。
