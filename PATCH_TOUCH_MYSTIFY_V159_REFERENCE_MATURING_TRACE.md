# PATCH_TOUCH_MYSTIFY_V159_REFERENCE_MATURING_TRACE

本次继续主动检查 V158 源码，并与用户提供的标准参考视频进行比对。

## 一、仍然存在的主要重大差距

V158 已经解决了很多早期问题，例如多主体分身、白色乱线团、主体边界不清等。但它仍然存在一个重大方向差距：

- V158 是“每一段选择一个固定 motif，然后把它画出来”；
- 标准参考视频更像“同一段光迹从小起笔开始，局部逐步成熟、展开、变形、退隐”。

换句话说，V158 更像清楚的光形短句展示；参考视频更像时间中正在成熟的光迹。

## 二、本次核心改造

主要文件：

- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

新增主入口：

- `drawReferenceVideoTimePainterV159(...)`

当前主渲染路径已经从 V158 切换到 V159。

## 三、V159 技术方案

### 1. 引入“出生时间”与“局部成熟度”

新增：

- `v159Point(...)`
- `v159SeedPoint(...)`
- `v159TargetPoint(...)`

每个可见点都会根据它在光迹中的位置推算出近似出生时间。
因此，同一主体中：

- 新头部仍然更像细线；
- 中段逐渐成熟为当前光形家族；
- 旧部分继续退隐。

这避免 V158 那种“整段一开始就是某个固定 motif”的观感。

### 2. 从“固定 motif”改为“家族内成熟变体”

V159 仍然保留参考视频中常见的清楚光形家族：

- 书写丝带
- 叶片/场线
- 宽薄翼面
- 钩形/花瓣
- 环形网纱
- 极简长弧

但每一类内部都有两个阶段：初型与成熟型。
主体不是直接替换完整形状，而是在同一个家族内部连续成熟。

### 3. 保持大留白与少量时间回声

V159 继续保持：

- 单一主体；
- 黑底大留白；
- 极淡同源时间回声；
- 克制的白色芯线；
- 只在场线/环网家族中生成少量内部纹理。

## 四、借鉴的开源屏保技术思路

本次继续借鉴：

- Windows Mystify 的“弯曲光线持续改变形状、颜色循环、短暂残影”思路；
- XScreenSaver 的 graphics hack 状态机模式：每帧推进绘制状态；
- rss-glx / Really Slick Screensavers 中 Flux、Euphoria、Fieldlines、Solarwinds 等强调光带、场线、粒子和流动结构的屏保思路。

## 五、目前仍需实机验证

V159 从源码逻辑上更接近参考视频的“光迹成熟过程”，但真实视觉效果仍需通过手机动态壁纸运行视频验证。
