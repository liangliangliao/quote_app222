# 足下一致行动 V3 产品闭环补强说明

本次 V3 在 V2 的基础上继续朝完整产品闭环升级，重点不是继续堆新页面，而是修复执行链路断点、增强统计与复盘、让数据能够从 AI 方案真正流向行动证据与报告。

## 关键升级

1. **奖励降噪 / 诚实校验方案可直接进入行动陪伴**
   - 修复 V2 中 `_rewardPlan`、`_selfCheckPlan` 只展示、不顺畅进入执行的问题。
   - 新增统一 `_activatePlan / _applyPlan`，所有场景生成的方案都能被设置为当前执行计划。

2. **证据保存补全来源链路与奖励上下文**
   - 行动证据现在会保存外部奖励上下文 `reward_source`。
   - 保存来源使用当前激活方案的 sourceType/sourceId，而不是只依赖页面初始来源。
   - 证据保存后会把关联 session 状态更新为 `evidence_saved`。

3. **行动时长记录更真实**
   - 新增“实际投入分钟”输入框。
   - 留空时按计时器实际经过时间计算；完全未计时时默认 1 分钟，不再把选择的 10 分钟直接记成已完成 10 分钟。
   - 暂停后继续不会重置首次开始时间。

4. **价值罗盘增强**
   - 支持编辑已有价值档案。
   - 每个价值档案展示关联行动证据数、累计分钟、最近证据。
   - 价值统计基于全部证据，而不是当前证据筛选结果。

5. **身份证据账本升级为成长仪表盘**
   - 新增顶部统计：证据数、累计分钟、回来次数、面对不适次数、来源链路数。
   - 统计最常被打破的旧解释、最常连接的价值。

6. **价值一致性报告支持周期范围**
   - 支持最近 7 天、最近 30 天、本月、全部行动证据。
   - 报告归档会保存具体周期标签。

7. **AI session 链路可查看和继续执行**
   - 报告页新增最近 AI 方案链路。
   - 展示 sceneType、status、目标、1 美元行动、provider/model。
   - 可一键继续执行历史 session 中的方案。

## 仍需真机验证

当前环境没有 flutter/dart 命令，未能执行真实 Flutter 编译。已完成静态源码改造、括号平衡检查和重新打包。
