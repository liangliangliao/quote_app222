# PATCH_MYSTIFY_REAL_STROKE_PREVIEW_V9

本补丁修正此前触摸模块预览仍然显示“两条预设曲线”的问题。

## 修正点

1. Flutter 设置页预览不再使用两个固定 anchor 曲线。
2. 预览改成随机 Stroke Event Generator：
   - 每条线从全屏可见范围任意位置出生。
   - 每条线由 2～6 个控制点即时生成。
   - 支持边缘切入、上下扫线、对角线、任意两点、角落切入、局部短弧。
   - 每条线有生命周期，出生、伸展、衰减、消失。
   - 用多次延迟绘制模拟残影。
3. Android 动态壁纸服务继续使用 OpenGL ES + FBO 残影版本。
4. 设置页文字改成“加速预览：全屏随机 Stroke 发生器”，避免误导为普通示意图。

## 注意

这次主要修复用户截图里看到的 App 内预览欺骗感：预览不能再是两条静态/预设曲线。真实动态壁纸仍以 Android 原生 OpenGL WallpaperService 为准。
