# V22 自动读取与当天重复读取修复说明

## 这次修复了什么

1. 明确修复“当天重复读取后看不到新数据”的问题：
   - 原先同一个外部数据源、同一个 external_id 已经确认后，再次读取会直接跳过。
   - 现在重新读取同一天同一应用/同一日历事件/同一健康摘要时，会刷新对应待确认项，让用户能在“待确认导入队列”看到最新读取结果。
   - 用户再次确认时，会按 `source + title + record_date_ms` 更新已有正式记录，避免重复累计时间。

2. 自动读取数据源扩展为三类：
   - 真实屏幕使用时间；
   - 真实系统日历；
   - Health Connect 健康数据。

3. Health Connect 健康数据支持每日自动读取：
   - 当前自动读取包含步数、睡眠分钟、运动分钟三个最核心身体状态指标；
   - 自动读取结果先进入待确认队列，不直接写入正式行为观察记录。

4. 数据源页的“每日自动读取配置”会出现三项：
   - 屏幕使用时间自动读取；
   - 系统日历自动读取；
   - 健康数据自动读取。

## 关于“是否限制当天重复读取”

是的，之前确实存在“同一天同一来源数据去重”的逻辑。这个逻辑本来是为了防止屏幕使用时间、日历等外部数据重复导入，导致复盘页时间膨胀。

但之前处理过于粗糙：如果同一条数据已经被确认，再次读取就直接跳过，所以用户会感觉“到点了却没有读取成功”。

V22 改成：

- 同一条外部数据重新读取时刷新待确认项；
- 用户确认后更新已有正式记录；
- 不再重复累加同一来源的同一天数据。

## 仍需真机验证

当前环境无法执行 Flutter / Android 真机编译测试。以下能力需要真机验证：

- UsageStatsManager 使用情况访问权限；
- READ_CALENDAR 日历读取权限；
- Health Connect 读取权限；
- 精确闹钟权限；
- App 后台/被杀死状态下自动读取是否准时触发。
