# PATCH_TOUCH_MYSTIFY_V167_VISIBLE_POLYMORPH_STRING

## 问题

用户在 V166 运行截图中只能看到一条很暗的线，几乎看不到“七十二变”的分叉、场线、变身层和形态变化。

这说明：源码中虽然计算了较多控制点变化、分叉、场线和回声，但它们在真实黑色动态壁纸背景下被过低的 alpha、过细的线宽和过暗的颜色吞掉了。

因此，问题不是“完全没有画”，而是：

- 大量内容画得太暗；
- 线宽过细；
- 旁路 lanes 只在 mutation 高时才出现；
- 场线与分叉 alpha 太低；
- 白色芯线与笔尖提示也偏弱；
- 真实手机壁纸环境中黑底 + 透明叠加会进一步降低可见性。

## 本次修复

### 1. 主色最低亮度保护

给 `mystifyPaletteFromHue(...)` 返回的主色设置最低亮度，避免颜色被黑底吞掉。

### 2. 提高主光弦线宽与透明度

修改 `drawV166PolymorphicString(...)`：

- 提高 `baseWidth`；
- 非省电模式默认至少 3 条轮廓 lane；
- mutation 时扩展到 5 条 lane；
- 主线 alpha 从偏低值提升到更可见；
- 旁路轮廓不再几乎不可见。

### 3. 提高变身分叉和场线可见度

修改：

- `drawV166MutationFieldThreads(...)`
- `drawV166MutationBranches(...)`

让分叉和场线在变身节拍出现时能真正被肉眼看见，而不是隐藏在黑底里。

### 4. 提高笔尖与起笔阶段亮度

修改：

- 起笔 seed ellipse；
- `drawV166BrushTip(...)`。

让当前正在创作的位置更清楚。

### 5. 拉长可见窗口

适当调整 `v166PhraseLife(...)` 与 `v166Tail(...)`：

- 更快进入可见状态；
- 更晚开始整体退隐；
- 尾部不要过早追上头部，避免画面只剩一小段线。

## 目标

V167 不是继续增加新的形态，而是先解决 V166 的最直接问题：

> 生成的内容被黑色背景和过低透明度吞掉，导致用户只能看到一条暗线。

本版应当让单一主体中的：

- 主光弦；
- 旁路轮廓；
- 分叉；
- 场线；
- 笔尖；
- 极淡时间切片；

都具备足够可见度。
