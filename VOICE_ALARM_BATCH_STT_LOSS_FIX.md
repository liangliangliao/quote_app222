# Voice Alarm batch STT loss fix

本次修复非实时语音识别长句/连续说话时“后半段丢失”的问题。

## 根因

1. 旧实现是串行链路：先录一段，再等待云端识别返回，然后才开始下一段录音。云端 REST/WebSocket 识别期间没有线程持续读取 AudioRecord，系统麦克风环形缓冲会覆盖旧音频，用户继续说的内容会实际没有被保存。
2. 非实时自动纠错的 token 上限固定为 160。长文本识别结果进入 AI 纠错后，纠错模型可能只输出前半段，导致看起来像 STT 丢了后半段。

## 修复

1. 非实时模式改为双线程：录音线程持续收音并把 PCM 段放入队列；识别线程独立消费队列。网络慢或上一段还在识别时，下一段语音仍会继续被录下。
2. 手动提交在上一段识别中也可使用，当前录音会加入识别队列，不再被“上一段正在识别”挡住。
3. 自动纠错 token 预算改为按文本长度动态计算，并加入截断保护：长文本纠错结果明显短于原始识别文本时，回退使用原始完整识别文本，避免纠错造成内容丢失。

## 修改文件

- `android/app/src/main/kotlin/com/example/quote_app/VoiceAlarmActivity.kt`
