# PATCH_TOUCH_MYSTIFY_V155_CLEAR_TIME_PAINTING

本次针对用户截图中出现的“白色乱线团 / 多根线互相缠绕 / 画面不清楚”继续修复。

## 一、问题原因

V154 虽然尝试做“时间绘画短句”，但内部参数过于激进：

1. 当前帧同时启用了过多 stream，普通模式多达约 20 条笔毛；
2. 回声切片过多，旧笔痕又被重新主动绘制；
3. 每条笔毛都有 lane jitter、birthShift 和独立局部样式变化，导致互相打架；
4. 白色核心高光太多，线条互相重叠后变成刺眼乱团；
5. Soft rib/肋线在多个位置出现，进一步造成“毛刺感”和“乱网感”；
6. 星芒/爆发模式容易产生短促、尖锐、杂乱的线团。

因此截图中看到的不是“清楚创作过程”，而是一团线条互相叠加。

## 二、本次修复方向

V155 的目标不是增加新效果，而是做减法：

- 减少并行笔毛；
- 减少回声切片；
- 降低旧笔痕透明度；
- 压低白色核心高光；
- 取消大部分肋线；
- 让风格变化慢速统一；
- 去掉最容易造成乱团的星芒爆发模式。

## 三、主要修改

文件：

- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

### 1. 收束主短句尺寸和宽度

降低：

- `phraseScale`
- `widthBase`

避免主体一开始就过大、过亮、过密。

### 2. 减少回声

从原来普通模式多层回声改为更少回声：

- 普通模式：2 层以内
- 省电模式：1 层以内

并降低回声 alpha。

### 3. 大幅减少 stream 数量

当前主笔毛从原先普通模式约 20 条，降低到约 7 条。

Echo 模式更少。

### 4. 限制 lane 宽度和 jitter

避免同一主体扩散成左右互相打架的线团。

### 5. 统一慢速风格变化

重写：

- `v154LocalPoint(...)`
- `v154LocalStyle(...)`
- `v154StyleIndex(...)`

风格变化从“每条笔毛各自乱跳”改成“同一短句内统一慢速过渡”。

### 6. 限制白色高光

只给中心主笔毛一条轻高光，防止满屏白色线条缠绕。

### 7. 大幅削弱肋线

`drawV154SoftRib(...)` 只在非常有限条件下出现，并减少长度、宽度和透明度。

## 四、同步更新

更新：

- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`

版本文案升级为 V155。
