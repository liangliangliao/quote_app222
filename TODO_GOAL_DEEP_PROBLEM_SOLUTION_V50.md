# V50 - To Do目标系统：AI深度问题解决方案升级

本次升级集中在 `lib/external_data/todo_goal_*` 模块，使 Microsoft To Do 同步目标和用户手动输入目标都能进入“AI深度问题解决”闭环。

## 核心新增能力

1. **目标即棘手问题**
   - To Do 任务或用户输入目标会被 AI 当作一个待解决的问题，而不是普通待办。
   - AI 输出结构化 JSON，包含目标意义、过程价值、今日最小行动，以及多套问题解决方案。

2. **多方案与备用方案**
   - 新增舒适区、拉伸区、恐慌区三类方案展示。
   - 用户可选择主方案；未选择方案保留为备用方案。
   - 方案包含科学方法、方法依据、核心价值焦点、风险边界。

3. **问题树/问题网结构**
   - 新增问题节点表，支持 `tree / linear / network` 关系。
   - 每套方案可包含顶层问题、子问题、更小子问题、底层可执行动作。
   - 用户从底层动作开始执行，再逐层回推父问题，直到整体目标实现。

4. **手动输入目标入口**
   - 不必先进入 Microsoft To Do；用户可以直接输入目标或棘手问题。
   - 系统同样生成目标档案、多方案、问题树和今日行动。

5. **节点成功/失败复盘**
   - 问题树中的任意节点可标记“成功”或“失败”。
   - 标记时要求用户输入现实反馈。
   - AI 会根据反馈生成节点复盘、重启指导和替代步骤。
   - 失败时可从 AI 替代步骤中选择一个加入今日行动，其余作为备用。

6. **今日行动失败恢复**
   - 今日复盘新增“成功/失败”选择。
   - 失败不再只是记录，而是触发 AI 诊断、重启指导和替代步骤生成。
   - 默认不建议重构整个方案；只有方向明显错误或多次验证无效时才提醒重构。

## 数据表新增

- `goal_solution_plans`：保存 AI 多方案、主方案/备用方案状态。
- `goal_problem_nodes`：保存方案问题树节点、父子关系、节点状态、AI节点复盘。
- `goal_step_reviews`：保存步骤/节点复盘、AI诊断、替代步骤。

## 主要修改文件

- `lib/external_data/todo_goal_models.dart`
- `lib/external_data/todo_goal_dao.dart`
- `lib/external_data/todo_goal_ai_service.dart`
- `lib/external_data/todo_goal_pages.dart`

## 注意

当前环境未安装 Flutter/Dart SDK，无法执行 `flutter analyze` 或真机编译；已完成静态结构检查、括号配平检查和关键代码路径核查。
