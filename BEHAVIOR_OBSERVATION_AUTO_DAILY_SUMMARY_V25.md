# V25 行为观察：每日摘要自动汇总修复

## 背景

用户指出：每日摘要不应该要求用户再手动新增一条记录。行为观察模块已经保存了当天行为事实，摘要页应该根据当天记录自动汇总，而不是让用户再填一次“每日六项”。

## 本次修复

### 1. 每日摘要改为自动汇总页

- 首页「每日摘要」入口不再打开新增记录表单。
- 点击后进入新的「每日摘要」页面。
- 页面会自动读取所选日期的行为记录，并生成摘要。
- 支持切换日期查看历史某天摘要。

### 2. 自动汇总内容

自动摘要包含：

- 当天记录数量；
- 有时长记录合计；
- 时间流向；
- 行为类别比重；
- 具体做了什么；
- 情绪线索；
- 触发/情境线索；
- 环境线索；
- 身体状态线索；
- 一段可读的自动摘要。

### 3. 不再强迫用户“新增每日摘要”

- 每日摘要默认只是一个动态汇总视图。
- 用户不需要填写标题、类别、标签、睡眠、重要行为等字段。
- 系统会根据当天已有记录自动生成。

### 4. 保留可选快照

如果用户希望把某天摘要作为固定档案保存，可以点击：

> 保存摘要快照（可选）

保存后会生成一条 `auto_daily_summary` 记录；该记录不会再次参与当天摘要计算，避免摘要套摘要。

## 修改文件

- `lib/behavior_tracking/behavior_tracking_home_page.dart`
- `lib/behavior_tracking/behavior_tracking_models.dart`

