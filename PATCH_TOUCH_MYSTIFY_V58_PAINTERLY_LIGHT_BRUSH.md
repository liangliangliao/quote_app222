# 触摸 / Mystify 动态壁纸 V58：光笔现场作画式生命光膜

本补丁基于 V57 继续深度改造主体阶段，使动画与“黑场中一支看不见的光笔正在实时作画”的文字描述对齐。

## 主要目标

标准视频的核心不是完整主体漂移，也不是叶片/丝带模板轮换，而是：

- 笔尖先出现一小段湿亮光线；
- 随笔尖推进，短历史轨迹逐渐长出透明膜面；
- 膜面中出现细密、均匀、柔和的丝纹/肋线；
- 外缘、折脊、内侧焦散线按年龄逐段变亮、变暗；
- 形体在运动中展开、回钩、扭腰、收束；
- 旧线不是突然消失，而是被黑场慢慢吞掉；
- 释放与余韵阶段保持原逻辑不动。

## 本次源码变化

主要修改文件：

- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`
- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`

## 渲染算法变化

1. 主体阶段仍禁用旧的多 StrokeEvent 生成，避免粗白带、黄带交叉、多主体脏影。
2. 每轮主体由一条随机 Catmull-Rom 样条路径作为“光笔轨迹”。
3. 当前帧主体不是完整模板，而是由 `drawSec` 向前回看 5.8～8.9 秒的短历史窗口生成。
4. 历史窗口随入场逐渐增长，所以开头只是一小段线，随后才慢慢展开为光膜。
5. 每个采样点按自己的历史时间重新计算展开、回钩、扭腰三组低频笔势，实现实时变形。
6. 膜面、外缘、折脊、焦散线均改为年龄分段绘制，尾迹淡、笔尖湿亮，中段柔软展开。
7. 肋线从明亮喉口向外缘拉开，不再像静态叶脉，而像被现场刷出的透明丝线。
8. 主体阶段恢复极短 FBO 湿迹回声，增强“现场作画”连续性，但不恢复长残影。

## 验证

已用轻量 Android / OpenGL / EGL 桩类对核心 Java 文件执行 `javac` 语法级检查，通过。

完整 Gradle/Flutter 构建仍受源码包缺少 `android/gradle/wrapper/gradle-wrapper.jar` 影响，当前离线环境未执行 APK 构建。
