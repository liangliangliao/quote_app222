# V24 现实主义乐观训练系统：日常状态机与跨模块回流补全

## 本次目标

基于最终产品设计方案，继续补齐 v23 日常闭环中仍不够严谨的部分：安全降级、复盘落库、待办事项回流、详情页完成入口一致性、防重复、环境清理个性化，以及数据结构可追踪性。

## 已落地修复

1. **L3/L4 安全降级更严格**
   - L3 高强度痛苦也会暂停普通晚间复盘和普通日常训练。
   - 安全模式会暂停相关日常闭环产物，并同步关闭已生成待办事项的提醒和“我的一天”标记。
   - 安全支持卡增加可信任联系人、安全地点、2 分钟稳定动作和现实支持资源提示。

2. **晚间复盘真实落库**
   - `daily_review` 场景现在也会写入具体感恩、品味练习和关系感谢数据。
   - 修复过去只有 `gratitude_savoring` 场景能进入感恩/品味表的问题。

3. **逾期待办事项不再自动等同失败**
   - 自动同步发现逾期未完成时，不直接把用户判定为失败。
   - 改为生成“待复盘卡片”，提供：已完成只是忘了点、没完成帮我拆小、暂时跳过、任务不重要了。
   - 同时创建一个 2 分钟恢复入口，保留重新开始的可能。

4. **详情页记录行动证据也会同步日常闭环**
   - 详情页点击“记录行动证据”时，会查找关联的日常产物和训练待办事项。
   - 如果有关联训练待办事项，会同步标记完成、更新日常产物状态、写入行动证据和身份沉淀。
   - 增加防重复提示，避免同一详情页反复记录同一条行动证据。

5. **防重复与替换逻辑增强**
   - 日常页关键按钮增加运行锁，避免连续点击生成多条重复产物。
   - 生成新的晚间复盘、明日注意力启动线索、今日环境清理时，会替换旧的同类未完成产物，并同步暂停旧待办事项提醒。

6. **行动证据来源可追踪**
   - `action_evidence` 增加来源字段：`source_type`、`source_id`、`todo_task_id`、`daily_artifact_id`。
   - 周报和证据页以后可以区分行动来自训练记录、日常产物、待办事项还是失败恢复。

7. **品味练习数据补全**
   - `savoring_entry` 增加 `body_feeling` 字段，支持记录 30 秒品味练习中的身体感受。

8. **可控失败挑战生命周期补强**
   - 挑战表增加 `challenge_status`、`planned_at_ms`、`user_reflection`、`next_attempt` 字段。
   - 为后续从“一次性结果卡”升级为“挑战生命周期”打基础。

9. **今日最小环境清理个性化**
   - 不再只生成固定模板。
   - 用户可以输入今天最容易启动拖延、比较、自责或无力感的触发源，系统生成更贴合的清理动作。

10. **主动提醒简单降噪**
   - 触发提醒会记录触发次数。
   - 多次触发后会自动把文案改得更小，例如从 5 分钟降到 2 分钟入口。

## 尚未完全实现的边界

- 真实系统通知的“用户忽略次数”仍需要平台通知回调支持；当前版本记录的是模块内触发次数。
- 可信任联系人、安全地点等配置入口尚未做成独立设置页，本版先在安全支持卡中明确提示。
- Flutter/Dart 编译需要在本地环境执行；当前修复完成源码级检查，但本环境没有 `flutter` / `dart` 命令。
