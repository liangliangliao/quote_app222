# V16 行为观察通知与提醒到期修复

## 修复点

1. 通知展开后只能显示 3 个层面的问题

Android 系统在多数机型上会限制单条通知可见 action 数量，原先把 7 个观察层面全部放进同一条通知 action，实际只会展示前 3 个。

V16 改为通知组方案：

- 1 条行为观察总提醒；
- 7 条子通知，分别对应：时间、行为、情绪、认知、结果、环境、身体；
- 每条子通知都有自己的 RemoteInput 输入框；
- 用户不打开 App，也可以直接在对应层面通知中输入并保存；
- 点击某个层面的子通知，则进入 App 内对应完整表单。

2. 对应表单提示更明确

每个层面通知现在显示不同表单提示：

- 时间：做了什么；开始-结束；类别；备注
- 行为：具体做了什么；为什么做；类别；备注
- 情绪：事件；情绪；强度；当时反应
- 认知：情境；自动想法；后续行为
- 结果：行为；短期结果；长期影响
- 环境：地点；身边人；物品/手机；影响
- 身体：睡眠；精力；疲劳；运动/步数

3. 点击层面通知打开正确表单

通知 payload 新增 primaryLayer，Flutter 路由会把该层面传给行为观察编辑页。

4. 预设时间到期收不到提醒的问题

原先新增提醒只是保存配置，用户还需要手动点击“注册未来7天”。V16 改为：

- 新增提醒后自动申请/检查通知权限；
- 检查 Android 精确闹钟权限；
- 权限可用时，自动注册未来 7 天提醒；
- 若精确闹钟权限未开启，提示用户授权后返回重新注册。

5. 自动读取注册也补充精确闹钟权限检查

屏幕使用时间/系统日历自动读取注册前，会先检查精确闹钟权限，避免到点无触发。

## 注意

通知组和 RemoteInput 的最终展示仍受不同 Android ROM 影响，例如 MIUI/ColorOS 可能把 7 条子通知折叠在“更多通知”里，需要用户展开通知组查看。源码层面已经避免了单条通知最多只显示 3 个 action 的限制。
