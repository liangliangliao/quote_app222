# 健康饮食模块核心闭环升级说明

本次不是继续堆页面，而是把前面阶段中处于“占位/本地演示”的核心能力打通：

## 已真正激活的链路

1. **API Key 生效**
   - `USDA API Key`：文字/语音/AI 图片识别出的食物，会尝试调用 USDA FoodData Central 搜索并缓存营养数据。
   - `Open Food Facts User-Agent`：条形码会调用 Open Food Facts `/api/v2/product/{barcode}.json` 查询包装食品营养、配料、过敏原、Nutri-Score/NOVA 等字段。
   - `Spoonacular API Key`：健康菜谱页会尝试调用 Spoonacular `recipes/complexSearch` 拉取在线菜谱、营养与制作信息。
   - `Edamam App ID/App Key`：健康菜谱页会尝试调用 Edamam Recipe Search API v2 拉取在线菜谱与健康标签。
   - `薄荷健康 API Key`：仍保留配置位。薄荷正式商用/接口鉴权策略通常需要平台授权文档，未在源码中硬编码不可确认的私有接口。

2. **每日饮食分享打通**
   - 文字：本地规则解析 + 全局 AI 解析 + USDA/Open Food Facts 营养补全。
   - 语音：语音转文字后走同一条文字解析链路。
   - 图片：上传图片后调用全局 AI 视觉模型识别食物；识别不确定时要求用户手动确认。
   - 条形码：输入条码后直接调用 Open Food Facts 查询包装食品，不再只是占位“包装食品”。

3. **复盘使用真实数据**
   - 确认页保存时，会把外部 API 返回的标准食物写入 `food_items`。
   - 今日饮食复盘读取 `normalized_food_id` 对应的营养数据，参与蛋白质、纤维、糖、钠、加工食品风险评分。

4. **菜谱不再只是本地数据**
   - 页面会先显示本地保底菜谱，同时尝试调用 Spoonacular / Edamam。
   - 在线返回成功后合并展示在线菜谱。
   - 接口失败或密钥为空时会显示状态说明，并保留本地保底菜谱避免页面空白。

## 仍需要注意

- 图片识别依赖当前全局 AI 模型是否支持视觉输入。如果选择的模型不支持图片，页面不会崩溃，但只能手动添加食物。
- Open Food Facts 是社区开放数据库，包装食品数据可能缺失或不完整。
- USDA 更适合基础食材，中文菜名可能命中率有限；中文食物需要后续结合薄荷授权接口或自建中文食物映射库。
- 饮食建议仍是健康参考，不能作为疾病治疗方案。
