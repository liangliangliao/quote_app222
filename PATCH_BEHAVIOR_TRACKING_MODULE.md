# 行为跟踪模块落地说明

本次在首页左侧菜单新增「行为跟踪」模块，并完成可运行的本地记录与复盘功能。

## 已实现

1. 首页左侧菜单新增入口
   - 入口标题：行为跟踪
   - 入口说明：时间日志 → 行为证据 → 情绪触发 → 认知结果 → 定期复盘
   - 点击后进入独立模块页面。

2. 新增本地数据表
   - 表名：behavior_tracking_records
   - 使用 SQLite 本地保存，不依赖外部服务。
   - 支持记录日期、时间段、类别、行为、原因、结果、情绪强度、触发点、自动想法、环境因素、身体状态、短期结果、长期影响、替代方案、每日模板字段等。

3. 新增记录能力
   - 普通完整观察记录：覆盖时间、行为、情绪、认知、结果、环境、身体状态。
   - 每日 6 项模板：睡眠、重要行为、时间浪费、情绪、触发点、明日调整。
   - 触发—行为—结果模板：触发、行为、短期结果、长期影响、替代方案。
   - 时间块记录：支持开始/结束时间，自动统计持续时间。
   - 类别快捷选择：工作/学习、娱乐、生活事务、休息、人际、健康、自律、消费、其他。

4. 复盘能力
   - 支持近 7 天 / 近 30 天切换。
   - 汇总记录数量、已记录时间块、平均情绪强度。
   - 按类别统计时间花费。
   - 自动整理触发点和替代方案线索。
   - 保留原始记录列表，便于复盘时回看证据。

5. 文件变更
   - 新增：lib/behavior_tracking/behavior_tracking_models.dart
   - 新增：lib/behavior_tracking/behavior_tracking_dao.dart
   - 新增：lib/behavior_tracking/behavior_tracking_home_page.dart
   - 修改：lib/main.dart，接入首页左侧菜单入口。

## 说明

当前运行环境未安装 dart/flutter 命令，因此无法在容器内执行 dart format 或 flutter analyze。已完成源码级静态结构检查、括号配对检查与打包。
