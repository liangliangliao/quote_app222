# PATCH_TOUCH_MYSTIFY_V90_REFERENCE_PEN_ART

本次根据用户提供的实机黑屏截图与参考视频，继续优化“发现之旅 / 生理赋能 / 触摸”的主体动画模块。

## 重点问题与原因

1. 刚安装 / 刚设为动态壁纸后长时间黑屏
   - V89 主体阶段使用 `local / 0.030` 做淡入。
   - 在真实动态壁纸中，一个主体阶段可能持续数小时甚至数天，3% 的阶段时间会被放大成很长时间，因此开场会几乎不可见。
   - V90 已改成真实秒级淡入：进入主体阶段约 0.6 秒内显形。

2. 光笔无休止连续画线
   - V89 的时间轴是连续 `gestureLen`，每段结束立刻进入下一段。
   - V90 改成“书写槽位”：每笔包含作画时间和停笔时间。
   - 停笔期只保留很轻的笔尖悬停微光，旧形体交给 FBO 余痕自然淡出。

3. 只有线条，缺少参考视频中的艺术形体
   - V89 只绘制笔尖附近的短湿迹，完整形体主要依靠残影累积，实机上容易看成单线。
   - V90 在每一笔中完整 reveal 当前形体：先出现主脊线，再逐步打开膜面、肋纹、边缘高光、结构线。

## 参考视频借鉴的作画特点

- 黑场中先出现一个小的光笔尖或短弧。
- 线条不是一直平移，而是“起笔、展开、停顿、消散、再出现”。
- 主形体通常是单体，不是满屏杂乱线团。
- 形体包含：丝绸弧刃、蝠翼/扇翼、折扇肋纹、开放环、回旋、花瓣/叶片、新月弯钩、水母伞、晶体切面、星链、极光光幕等。
- 美感来自主边缘高光、半透明膜面、细密肋纹、暗处余影和黑场留白。

## 修改文件

- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`
- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`
- `lib/pages/touch_mystify_wallpaper_page.dart`

## 核心改动

- `drawContinuousPenDrawingSubject(...)`
  - 修复开场淡入：从阶段比例淡入改为真实秒级淡入。
  - 新增作画 / 停笔节奏，避免持续无间隔画线。
  - 首帧增加 0.86 秒可见起笔偏移，避免从绝对零点黑场开始。
  - 每笔绘制完整 reveal，不再只绘制笔尖附近短线。

- 新增 `drawPenRestHold(...)`
  - 停笔期只显示微弱笔尖悬停光。

- 新增 `drawPenArtShapeScaffold(...)`
  - 按 motif 类型生成结构层：
    - 扇翼 / 水母伞 / 花冠：主根到外缘的肋纹。
    - 开放环 / 螺旋 / 羽眼：内外边缘与中间轮廓线。
    - 晶体 / 折纸：切面支撑线。
    - 星链：发光节点。
    - 丝带 / 花瓣 / 书法钩：半透明膜面、双边高光、短肋纹。

## 验证

- 对修改后的 Java 文件进行了本地 stub 语法编译检查，未发现 Java 语法错误。
- 本环境无法联网下载 Gradle wrapper / Android SDK，因此未生成 APK。
