# 足下一致行动 V8 可见化补全说明

本版本针对实机反馈修复：预设价值标签过去看起来像普通按钮，用户难以确认多选；补充功能入口也不够显性。

## 本次可见化补强

1. 预设价值区改为「预设价值（可多选）」卡片。
2. 每个预设价值使用 FilterChip，多选后显示勾选状态与 ✓ 前缀。
3. 新增「全选预设」按钮，便于验证多价值多选链路。
4. 多价值选择面板保留：扫描协同/冲突、带入失调雷达、直接生成行动。
5. 顶部功能入口保留：失调雷达、行动陪伴、证据账本、奖励降噪、诚实校验、一致性报告。

## 重要说明

如果实机仍只显示普通按钮，没有「预设价值（可多选）」标题、没有「全选预设」按钮、没有勾选状态，说明安装的不是该 V8 包构建出来的 APK。
