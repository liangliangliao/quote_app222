# PATCH_TOUCH_MYSTIFY_V132_TIME_PAINTING_FROM_VIDEO

本次基于用户提供的 Windows7 Mystify screensaver 实拍视频继续优化“发现之旅 / 生理赋能 / 触摸”动画主体。

## 一、视频观察结论

从视频抽帧观察，参考效果并不是“画好一个主体后让它在屏幕上漂移”，而是持续进行时间绘画：

1. 黑色背景中每隔一段时间会出现新的光形短句；
2. 每个短句的位置、尺度、方向和形态都明显不同；
3. 有的短句极简，只有一条细长光线；
4. 有的短句像丝带、羽翼、花瓣、弯钩、光刃；
5. 有的短句带有淡淡网纱或历史切片；
6. 旧形态会快速淡去，新形态继续生成；
7. 观感的核心不是复杂，而是“你不知道下一次它会生成什么”。

因此，当前源码不能继续只理解为“一个光弦主体的运动”，必须改为“时间短句持续创作”。

## 二、V132 核心改造方向

文件：
- `android/app/src/main/java/com/example/quote_app/wallpaper/IntimacyMystifyWallpaperService.java`

### 1. 重新校准为“时间绘画”模型
在 `drawScreensaverArtistBrush(...)` 中更新 V132 注释与参数：

- 光弦数量控制为省电 1 条，普通模式 3 条；
- 残影层数调整为省电 16、普通模式 26；
- 让残影间距受速度和短句脉冲共同影响；
- 让主光弦保持清晰，副光弦只提供层次。

### 2. 每个短句重新选择构图中心
新增：
- `mystifyPhraseScreenCenter(...)`

不再让光弦总围绕上层 sourceStroke 中心轻微漂移，而是每个短句都有自己新的屏幕构图中心。

这样更接近视频中“下一次出现在完全不同位置、生成完全不同内容”的效果。

### 3. 每个短句重新选择舞台尺度
改造：
- `mystifyPhraseStageScale(...)`

现在每段短句直接返回相对屏幕 minDim 的舞台半径，尺度变化更明显。

### 4. 每个短句可拥有不同控制点数量
新增：
- `mystifyPhraseControlCount(...)`

不同形态语法会使用不同数量的控制点，避免所有主体都像同一个结构在变形。

### 5. 变形节奏改为“蓄势—变化—稳定”
改造：
- `mystifyPhraseDuration(...)`
- `mystifyPhraseMorphMix(...)`

让一个形态有足够时间被看见，然后中段进行丝滑变形，而不是一直过快乱跳或一直停留。

### 6. 新增贴附于当前光弦的时间绘画层
新增：
- `drawMystifyTimePaintingAccent(...)`

这个方法不会生成独立漂移的图形，而是把丝带、薄幕、扇形肋线、局部亮点都贴附在当前光弦上。

根据当前短句模式，会生成不同附加语汇：

- 薄丝带面；
- 扇形肋线；
- 局部亮核与笔尖；
- 柔性薄幕。

这样可以增强视频中的艺术变化，但避免再次出现“线和形状分离”。

### 7. 角度插值改为真正圆周插值
新增：
- `lerpAngle(...)`

相邻短句的方向过渡不再直接线性插值角度，减少突然反转或绕远。

## 三、同步页面文案

已同步更新：
- `lib/pages/physical_enhancement_page.dart`
- `lib/pages/discover_page.dart`

版本文案升级为 V132。

## 四、注意

本环境没有 Android SDK，因此只进行了源码静态检查和打包，没有在此处生成 APK。
